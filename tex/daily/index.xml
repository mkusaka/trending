<?xml version="1.0" encoding="UTF-8"?><feed xmlns="http://www.w3.org/2005/Atom">
  <title>GitHub TeX Daily Trending</title>
  <id>http://mshibanami.github.io/GitHubTrendingRSS</id>
  <updated>2022-11-29T01:36:50Z</updated>
  <subtitle>Daily Trending of TeX in GitHub</subtitle>
  <link href="http://mshibanami.github.io/GitHubTrendingRSS"></link>
  <entry>
    <title>fjeremic/fjeremic.github.io</title>
    <updated>2022-11-29T01:36:50Z</updated>
    <id>tag:github.com,2022-11-29:/fjeremic/fjeremic.github.io</id>
    <link href="https://github.com/fjeremic/fjeremic.github.io" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Personal website of Filip Jeremic.&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;jeremic.ca&lt;/h1&gt; &#xA;&lt;p&gt;Personal website of Filip Jeremic. Statically built using &lt;a href=&#34;https://gohugo.io/&#34;&gt;Hugo&lt;/a&gt; and &lt;a href=&#34;https://git.io/hugo-congo&#34;&gt;congo&lt;/a&gt;. Deployed using &lt;a href=&#34;https://github.com/features/actions&#34;&gt;GitHub Actions&lt;/a&gt; and &lt;a href=&#34;https://pages.github.com/&#34;&gt;GitHub Pages&lt;/a&gt; in a Dockerized environment.&lt;/p&gt; &#xA;&lt;h2&gt;Docker&lt;/h2&gt; &#xA;&lt;p&gt;The Dockerfile in the root of the repository builds a docker image with the exact dependencies needed to generate the static website. The GitHub Actions automation which builds and deploys the website is built using the same docker image.&lt;/p&gt; &#xA;&lt;h3&gt;Setup&lt;/h3&gt; &#xA;&lt;p&gt;We first build the docker image:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;docker build . -t jeremic.ca&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Next we initialize the git submodule which will clone the Hugo theme:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;git submodule init&#xA;git submodule update&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Next we need to install the theme dependencies by mounting the current repository inside the docker container:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;docker run -v $PWD:/$PWD -w $PWD/themes/congo -it jeremic.ca npm install&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Build&lt;/h3&gt; &#xA;&lt;p&gt;To build the site we need to first invoke the Tailwind CSS JIT compiler to parse the website and theme contents and generate the CSS file from a union set of all CSS classes used. Following the CSS generation we invoke the Hugo build system to generate the static website.&lt;/p&gt; &#xA;&lt;p&gt;We can use the same script that is used by the automation to accomplish this:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;docker run -v $PWD:/$PWD -w $PWD -it jeremic.ca .github/actions/hugo-build/action.sh&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Alternatively we can manually invoke the two steps:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;docker run -v $PWD:/$PWD -w $PWD -it jeremic.ca npm run tailwind-build&#xA;docker run -v $PWD:/$PWD -w $PWD -it jeremic.ca npm run hugo-build&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The static website is generated in the &lt;code&gt;./public&lt;/code&gt; directory.&lt;/p&gt; &#xA;&lt;h3&gt;Watch&lt;/h3&gt; &#xA;&lt;p&gt;When making changes to the website it is inconvenient to have to build and serve the website after each change. Instead we can launch two processes in our docker container which will watch for any changes to our website and automatically rebuild and serve the content. We can use two terminal shells to invoke the following two commands:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;docker run -v $PWD:/$PWD -w $PWD -it jeremic.ca npm run tailwind-watch&#xA;docker run -v $PWD:/$PWD -w $PWD -p 8000:8000 -it jeremic.ca npm run hugo-watch&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;LaTeX&lt;/h2&gt; &#xA;&lt;p&gt;We purposely don&#39;t install the Linux LaTex packages in the Dockerfile because it bloats the image size unnecessarily. We can install the LaTex packages manually and build the Resume inside the docker container as follows:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;# Run the docker container as an interactive shell&#xA;docker run -v $PWD:/$PWD -w $PWD -it jeremic.ca /bin/bash&#xA;&#xA;# Install LaTeX&#xA;apt-get install -y texlive texstudio&#xA;&#xA;# Build the Resume&#xA;pdflatex Filip-Jeremic-Resume.tex&#xA;&lt;/code&gt;&lt;/pre&gt;</summary>
  </entry>
</feed>