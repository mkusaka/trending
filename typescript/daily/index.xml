<?xml version="1.0" encoding="UTF-8"?><feed xmlns="http://www.w3.org/2005/Atom">
  <title>GitHub TypeScript Daily Trending</title>
  <id>http://mshibanami.github.io/GitHubTrendingRSS</id>
  <updated>2024-08-04T01:34:55Z</updated>
  <subtitle>Daily Trending of TypeScript in GitHub</subtitle>
  <link href="http://mshibanami.github.io/GitHubTrendingRSS"></link>
  <entry>
    <title>Tonejs/Tone.js</title>
    <updated>2024-08-04T01:34:55Z</updated>
    <id>tag:github.com,2024-08-04:/Tonejs/Tone.js</id>
    <link href="https://github.com/Tonejs/Tone.js" rel="alternate"></link>
    <summary type="html">&lt;p&gt;A Web Audio framework for making interactive music in the browser.&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;Tone.js&lt;/h1&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://codecov.io/gh/Tonejs/Tone.js&#34;&gt;&lt;img src=&#34;https://codecov.io/gh/Tonejs/Tone.js/branch/dev/graph/badge.svg?sanitize=true&#34; alt=&#34;codecov&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;Tone.js is a Web Audio framework for creating interactive music in the browser. The architecture of Tone.js aims to be familiar to both musicians and audio programmers creating web-based audio applications. On the high-level, Tone offers common DAW (digital audio workstation) features like a global transport for synchronizing and scheduling events as well as prebuilt synths and effects. Additionally, Tone provides high-performance building blocks to create your own synthesizers, effects, and complex control signals.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://tonejs.github.io/docs/&#34;&gt;API&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://tonejs.github.io/examples/&#34;&gt;Examples&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h1&gt;Installation&lt;/h1&gt; &#xA;&lt;p&gt;There are two ways to incorporate Tone.js into a project. First, it can be installed locally into a project using &lt;code&gt;npm&lt;/code&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;npm install tone      // Install the latest stable version&#xA;npm install tone@next // Or, alternatively, use the &#39;next&#39; version&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Add Tone.js to a project using the JavaScript &lt;code&gt;import&lt;/code&gt; syntax:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import * as Tone from &#34;tone&#34;;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Tone.js is also hosted at unpkg.com. It can be added directly within an HTML document, as long as it precedes any project scripts. &lt;a href=&#34;https://github.com/Tonejs/Tone.js/raw/master/examples/simpleHtml.html&#34;&gt;See the example here&lt;/a&gt; for more details.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-html&#34;&gt;&amp;lt;script src=&#34;http://unpkg.com/tone&#34;&amp;gt;&amp;lt;/script&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;Hello Tone&lt;/h1&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;//create a synth and connect it to the main output (your speakers)&#xA;const synth = new Tone.Synth().toDestination();&#xA;&#xA;//play a middle &#39;C&#39; for the duration of an 8th note&#xA;synth.triggerAttackRelease(&#34;C4&#34;, &#34;8n&#34;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Tone.Synth&lt;/h2&gt; &#xA;&lt;p&gt;&lt;code&gt;Tone.Synth&lt;/code&gt; is a basic synthesizer with a single oscillator and an ADSR envelope.&lt;/p&gt; &#xA;&lt;h3&gt;triggerAttack / triggerRelease&lt;/h3&gt; &#xA;&lt;p&gt;&lt;code&gt;triggerAttack&lt;/code&gt; starts the note (the amplitude is rising), and &lt;code&gt;triggerRelease&lt;/code&gt; is when the amplitude is going back to 0 (i.e. &lt;strong&gt;note off&lt;/strong&gt;).&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const synth = new Tone.Synth().toDestination();&#xA;const now = Tone.now();&#xA;// trigger the attack immediately&#xA;synth.triggerAttack(&#34;C4&#34;, now);&#xA;// wait one second before triggering the release&#xA;synth.triggerRelease(now + 1);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;triggerAttackRelease&lt;/h3&gt; &#xA;&lt;p&gt;&lt;code&gt;triggerAttackRelease&lt;/code&gt; is a combination of &lt;code&gt;triggerAttack&lt;/code&gt; and &lt;code&gt;triggerRelease&lt;/code&gt;&lt;/p&gt; &#xA;&lt;p&gt;The first argument to the note which can either be a frequency in hertz (like &lt;code&gt;440&lt;/code&gt;) or as &#34;pitch-octave&#34; notation (like &lt;code&gt;&#34;D#2&#34;&lt;/code&gt;).&lt;/p&gt; &#xA;&lt;p&gt;The second argument is the duration that the note is held. This value can either be in seconds, or as a &lt;a href=&#34;https://github.com/Tonejs/Tone.js/wiki/Time&#34;&gt;tempo-relative value&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;The third (optional) argument of &lt;code&gt;triggerAttackRelease&lt;/code&gt; is &lt;em&gt;when&lt;/em&gt; along the AudioContext time the note should play. It can be used to schedule events in the future.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const synth = new Tone.Synth().toDestination();&#xA;const now = Tone.now();&#xA;synth.triggerAttackRelease(&#34;C4&#34;, &#34;8n&#34;, now);&#xA;synth.triggerAttackRelease(&#34;E4&#34;, &#34;8n&#34;, now + 0.5);&#xA;synth.triggerAttackRelease(&#34;G4&#34;, &#34;8n&#34;, now + 1);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Time&lt;/h2&gt; &#xA;&lt;p&gt;Web Audio has advanced, sample accurate scheduling capabilities. The AudioContext time is what the Web Audio API uses to schedule events, starts at 0 when the page loads and counts up in &lt;strong&gt;seconds&lt;/strong&gt;.&lt;/p&gt; &#xA;&lt;p&gt;&lt;code&gt;Tone.now()&lt;/code&gt; gets the current time of the AudioContext.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;setInterval(() =&amp;gt; console.log(Tone.now()), 100);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Tone.js abstracts away the AudioContext time. Instead of defining all values in seconds, any method which takes time as an argument can accept a number or a string. For example &lt;code&gt;&#34;4n&#34;&lt;/code&gt; is a quarter-note, &lt;code&gt;&#34;8t&#34;&lt;/code&gt; is an eighth-note triplet, and &lt;code&gt;&#34;1m&#34;&lt;/code&gt; is one measure.&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://github.com/Tonejs/Tone.js/wiki/Time&#34;&gt;Read about Time encodings&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;Starting Audio&lt;/h1&gt; &#xA;&lt;p&gt;&lt;strong&gt;IMPORTANT&lt;/strong&gt;: Browsers will not play &lt;em&gt;any&lt;/em&gt; audio until a user clicks something (like a play button). Run your Tone.js code only after calling &lt;code&gt;Tone.start()&lt;/code&gt; from a event listener which is triggered by a user action such as &#34;click&#34; or &#34;keydown&#34;.&lt;/p&gt; &#xA;&lt;p&gt;&lt;code&gt;Tone.start()&lt;/code&gt; returns a promise, the audio will be ready only after that promise is resolved. Scheduling or playing audio before the AudioContext is running will result in silence or incorrect scheduling.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;//attach a click listener to a play button&#xA;document.querySelector(&#34;button&#34;)?.addEventListener(&#34;click&#34;, async () =&amp;gt; {&#xA;&#x9;await Tone.start();&#xA;&#x9;console.log(&#34;audio is ready&#34;);&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;Scheduling&lt;/h1&gt; &#xA;&lt;h2&gt;Transport&lt;/h2&gt; &#xA;&lt;p&gt;&lt;code&gt;Tone.getTransport()&lt;/code&gt; returns the main timekeeper. Unlike the AudioContext clock, it can be started, stopped, looped and adjusted on the fly. You can think of it like the arrangement view in a Digital Audio Workstation.&lt;/p&gt; &#xA;&lt;p&gt;Multiple events and parts can be arranged and synchronized along the Transport. &lt;code&gt;Tone.Loop&lt;/code&gt; is a simple way to create a looped callback that can be scheduled to start and stop.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;// create two monophonic synths&#xA;const synthA = new Tone.FMSynth().toDestination();&#xA;const synthB = new Tone.AMSynth().toDestination();&#xA;//play a note every quarter-note&#xA;const loopA = new Tone.Loop((time) =&amp;gt; {&#xA;&#x9;synthA.triggerAttackRelease(&#34;C2&#34;, &#34;8n&#34;, time);&#xA;}, &#34;4n&#34;).start(0);&#xA;//play another note every off quarter-note, by starting it &#34;8n&#34;&#xA;const loopB = new Tone.Loop((time) =&amp;gt; {&#xA;&#x9;synthB.triggerAttackRelease(&#34;C4&#34;, &#34;8n&#34;, time);&#xA;}, &#34;4n&#34;).start(&#34;8n&#34;);&#xA;// all loops start when the Transport is started&#xA;Tone.getTransport().start();&#xA;// ramp up to 800 bpm over 10 seconds&#xA;Tone.getTransport().bpm.rampTo(800, 10);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Since Javascript callbacks are &lt;strong&gt;not precisely timed&lt;/strong&gt;, the sample-accurate time of the event is passed into the callback function. &lt;strong&gt;Use this time value to schedule the events&lt;/strong&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;Instruments&lt;/h1&gt; &#xA;&lt;p&gt;There are numerous synths to choose from including &lt;code&gt;Tone.FMSynth&lt;/code&gt;, &lt;code&gt;Tone.AMSynth&lt;/code&gt; and &lt;code&gt;Tone.NoiseSynth&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;All of these instruments are &lt;strong&gt;monophonic&lt;/strong&gt; (single voice) which means that they can only play one note at a time.&lt;/p&gt; &#xA;&lt;p&gt;To create a &lt;strong&gt;polyphonic&lt;/strong&gt; synthesizer, use &lt;code&gt;Tone.PolySynth&lt;/code&gt;, which accepts a monophonic synth as its first parameter and automatically handles the note allocation so you can pass in multiple notes. The API is similar to the monophonic synths, except &lt;code&gt;triggerRelease&lt;/code&gt; must be given a note or array of notes.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const synth = new Tone.PolySynth(Tone.Synth).toDestination();&#xA;const now = Tone.now();&#xA;synth.triggerAttack(&#34;D4&#34;, now);&#xA;synth.triggerAttack(&#34;F4&#34;, now + 0.5);&#xA;synth.triggerAttack(&#34;A4&#34;, now + 1);&#xA;synth.triggerAttack(&#34;C5&#34;, now + 1.5);&#xA;synth.triggerAttack(&#34;E5&#34;, now + 2);&#xA;synth.triggerRelease([&#34;D4&#34;, &#34;F4&#34;, &#34;A4&#34;, &#34;C5&#34;, &#34;E5&#34;], now + 4);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;Samples&lt;/h1&gt; &#xA;&lt;p&gt;Sound generation is not limited to synthesized sounds. You can also load a sample and play that back in a number of ways. &lt;code&gt;Tone.Player&lt;/code&gt; is one way to load and play back an audio file.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const player = new Tone.Player(&#xA;&#x9;&#34;https://tonejs.github.io/audio/berklee/gong_1.mp3&#34;&#xA;).toDestination();&#xA;Tone.loaded().then(() =&amp;gt; {&#xA;&#x9;player.start();&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;&lt;code&gt;Tone.loaded()&lt;/code&gt; returns a promise which resolves when &lt;em&gt;all&lt;/em&gt; audio files are loaded. It&#39;s a helpful shorthand instead of waiting on each individual audio buffer&#39;s &lt;code&gt;onload&lt;/code&gt; event to resolve.&lt;/p&gt; &#xA;&lt;h2&gt;Tone.Sampler&lt;/h2&gt; &#xA;&lt;p&gt;Multiple samples can also be combined into an instrument. If you have audio files organized by note, &lt;code&gt;Tone.Sampler&lt;/code&gt; will pitch shift the samples to fill in gaps between notes. So for example, if you only have every 3rd note on a piano sampled, you could turn that into a full piano sample.&lt;/p&gt; &#xA;&lt;p&gt;Unlike the other synths, Tone.Sampler is polyphonic so doesn&#39;t need to be passed into Tone.PolySynth&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const sampler = new Tone.Sampler({&#xA;&#x9;urls: {&#xA;&#x9;&#x9;C4: &#34;C4.mp3&#34;,&#xA;&#x9;&#x9;&#34;D#4&#34;: &#34;Ds4.mp3&#34;,&#xA;&#x9;&#x9;&#34;F#4&#34;: &#34;Fs4.mp3&#34;,&#xA;&#x9;&#x9;A4: &#34;A4.mp3&#34;,&#xA;&#x9;},&#xA;&#x9;release: 1,&#xA;&#x9;baseUrl: &#34;https://tonejs.github.io/audio/salamander/&#34;,&#xA;}).toDestination();&#xA;&#xA;Tone.loaded().then(() =&amp;gt; {&#xA;&#x9;sampler.triggerAttackRelease([&#34;Eb4&#34;, &#34;G4&#34;, &#34;Bb4&#34;], 4);&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;Effects&lt;/h1&gt; &#xA;&lt;p&gt;In the above examples, the sources were always connected directly to the &lt;code&gt;Destination&lt;/code&gt;, but the output of the synth could also be routed through one (or more) effects before going to the speakers.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const player = new Tone.Player({&#xA;&#x9;url: &#34;https://tonejs.github.io/audio/berklee/gurgling_theremin_1.mp3&#34;,&#xA;&#x9;loop: true,&#xA;&#x9;autostart: true,&#xA;});&#xA;//create a distortion effect&#xA;const distortion = new Tone.Distortion(0.4).toDestination();&#xA;//connect a player to the distortion&#xA;player.connect(distortion);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The connection routing is flexible, connections can run serially or in parallel.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const player = new Tone.Player({&#xA;&#x9;url: &#34;https://tonejs.github.io/audio/drum-samples/loops/ominous.mp3&#34;,&#xA;&#x9;autostart: true,&#xA;});&#xA;const filter = new Tone.Filter(400, &#34;lowpass&#34;).toDestination();&#xA;const feedbackDelay = new Tone.FeedbackDelay(0.125, 0.5).toDestination();&#xA;&#xA;// connect the player to the feedback delay and filter in parallel&#xA;player.connect(filter);&#xA;player.connect(feedbackDelay);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Multiple nodes can be connected to the same input enabling sources to share effects. &lt;code&gt;Tone.Gain&lt;/code&gt; is useful utility node for creating complex routing.&lt;/p&gt; &#xA;&lt;h1&gt;Signals&lt;/h1&gt; &#xA;&lt;p&gt;Like the underlying Web Audio API, Tone.js is built with audio-rate signal control over nearly everything. This is a powerful feature which allows for sample-accurate synchronization and scheduling of parameters.&lt;/p&gt; &#xA;&lt;p&gt;&lt;code&gt;Signal&lt;/code&gt; properties have a few built in methods for creating automation curves.&lt;/p&gt; &#xA;&lt;p&gt;For example, the &lt;code&gt;frequency&lt;/code&gt; parameter on &lt;code&gt;Oscillator&lt;/code&gt; is a Signal so you can create a smooth ramp from one frequency to another.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-javascript&#34;&gt;const osc = new Tone.Oscillator().toDestination();&#xA;// start at &#34;C4&#34;&#xA;osc.frequency.value = &#34;C4&#34;;&#xA;// ramp to &#34;C2&#34; over 2 seconds&#xA;osc.frequency.rampTo(&#34;C2&#34;, 2);&#xA;// start the oscillator for 2 seconds&#xA;osc.start().stop(&#34;+3&#34;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;AudioContext&lt;/h1&gt; &#xA;&lt;p&gt;Tone.js creates an AudioContext when it loads and shims it for maximum browser compatibility using &lt;a href=&#34;https://github.com/chrisguttandin/standardized-audio-context&#34;&gt;standardized-audio-context&lt;/a&gt;. The AudioContext can be accessed at &lt;code&gt;Tone.getContext&lt;/code&gt;. Or set your own AudioContext using &lt;code&gt;Tone.setContext(audioContext)&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;MIDI&lt;/h1&gt; &#xA;&lt;p&gt;To use MIDI files, you&#39;ll first need to convert them into a JSON format which Tone.js can understand using &lt;a href=&#34;https://tonejs.github.io/Midi/&#34;&gt;Midi&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;Performance&lt;/h1&gt; &#xA;&lt;p&gt;Tone.js makes extensive use of the native Web Audio Nodes such as the GainNode and WaveShaperNode for all signal processing, which enables Tone.js to work well on both desktop and mobile browsers.&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://github.com/Tonejs/Tone.js/wiki/Performance&#34;&gt;This wiki&lt;/a&gt; article has some suggestions related to performance for best practices.&lt;/p&gt; &#xA;&lt;h1&gt;Testing&lt;/h1&gt; &#xA;&lt;p&gt;Tone.js runs an extensive test suite using &lt;a href=&#34;https://mochajs.org/&#34;&gt;mocha&lt;/a&gt; and &lt;a href=&#34;http://chaijs.com/&#34;&gt;chai&lt;/a&gt; with nearly 100% coverage. Passing builds on the &#39;dev&#39; branch are published on npm as &lt;code&gt;tone@next&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;Contributing&lt;/h1&gt; &#xA;&lt;p&gt;There are many ways to contribute to Tone.js. Check out &lt;a href=&#34;https://github.com/Tonejs/Tone.js/wiki/Contributing&#34;&gt;this wiki&lt;/a&gt; if you&#39;re interested.&lt;/p&gt; &#xA;&lt;h1&gt;References and Inspiration&lt;/h1&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/cwilso&#34;&gt;Many of Chris Wilson&#39;s Repositories&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/mohayonao&#34;&gt;Many of Mohayonao&#39;s Repositories&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;http://webaudio.github.io/web-audio-api/&#34;&gt;The Spec&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;http://www.soundonsound.com/sos/may99/articles/synthsec.htm&#34;&gt;Sound on Sound - Synth Secrets&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;http://msp.ucsd.edu/techniques.htm&#34;&gt;Miller Puckette - Theory and Techniques of Electronic Music&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/chrisguttandin/standardized-audio-context&#34;&gt;standardized-audio-context&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt;</summary>
  </entry>
</feed>