<?xml version="1.0" encoding="UTF-8"?><feed xmlns="http://www.w3.org/2005/Atom">
  <title>GitHub TypeScript Weekly Trending</title>
  <id>http://mshibanami.github.io/GitHubTrendingRSS</id>
  <updated>2024-08-04T01:45:04Z</updated>
  <subtitle>Weekly Trending of TypeScript in GitHub</subtitle>
  <link href="http://mshibanami.github.io/GitHubTrendingRSS"></link>
  <entry>
    <title>johannesjo/super-productivity</title>
    <updated>2024-08-04T01:45:04Z</updated>
    <id>tag:github.com,2024-08-04:/johannesjo/super-productivity</id>
    <link href="https://github.com/johannesjo/super-productivity" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Super Productivity is an advanced todo list app with integrated Timeboxing and time tracking capabilities. It also comes with integrations for Jira, Gitlab, GitHub and Open Project.&lt;/p&gt;&lt;hr&gt;&lt;a href=&#34;https://super-productivity.com/&#34;&gt; &lt;img align=&#34;center&#34; alt=&#34;Banner&#34; src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/banner-bw.png&#34;&gt; &lt;/a&gt; &#xA;&lt;p align=&#34;center&#34;&gt; &lt;strong&gt;An advanced todo list app with timeboxing &amp;amp; time tracking capabilities that supports importing tasks from your calendar, jira, github and others&lt;/strong&gt; &lt;/p&gt;&#xA;&lt;p&gt; &lt;/p&gt;&#xA;&lt;p align=&#34;center&#34;&gt;&lt;span&gt;🌐&lt;/span&gt; &lt;a href=&#34;https://app.super-productivity.com&#34;&gt;Open Web App&lt;/a&gt; or &lt;span&gt;💻&lt;/span&gt; &lt;a href=&#34;https://github.com/johannesjo/super-productivity/releases&#34;&gt;Download&lt;/a&gt;&lt;/p&gt; &#xA;&lt;br&gt; &#xA;&lt;p align=&#34;center&#34;&gt; &lt;a href=&#34;https://youtu.be/_mvMXWLsL-8&#34;&gt; &lt;img src=&#34;https://github.com/user-attachments/assets/15384cae-8ba3-4474-bc2c-13e7765c2dd1&#34; alt=&#34;animated&#34;&gt; &lt;/a&gt; &lt;/p&gt; &#xA;&lt;br&gt; &#xA;&lt;br&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://vshymanskyy.github.io/StandWithUkraine&#34;&gt;&lt;img src=&#34;https://raw.githubusercontent.com/vshymanskyy/StandWithUkraine/main/banner2-direct.svg?sanitize=true&#34; alt=&#34;Stand With Ukraine&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p align=&#34;center&#34; style=&#34;text-align: center;&#34;&gt; &lt;a href=&#34;https://lbesson.mit-license.org&#34;&gt; &lt;img alt=&#34;MIT license&#34; src=&#34;https://img.shields.io/badge/License-MIT-blue.svg?style=flat-square&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://github.com/johannesjo/super-productivity/releases&#34;&gt; &lt;img alt=&#34;GitHub All Releases&#34; src=&#34;https://img.shields.io/github/downloads/johannesjo/super-productivity/total&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://community.chocolatey.org/packages/super-productivity&#34;&gt; &lt;img alt=&#34;Chocolatey package&#34; src=&#34;https://img.shields.io/chocolatey/dt/super-productivity?color=blue&amp;amp;label=chocolatey&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://www.producthunt.com/posts/super-productivity&#34;&gt; &lt;img alt=&#34;On product hunt&#34; src=&#34;https://img.shields.io/badge/on-product%20hunt-blue.svg?style=flat-square&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://www.reddit.com/r/superProductivity/&#34;&gt; &lt;img alt=&#34;Reddit Community&#34; src=&#34;https://img.shields.io/badge/Reddit-%23FF4500.svg?style=for-the-badge&amp;amp;logo=Reddit&amp;amp;logoColor=white&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://mastodon.social/@superproductivity&#34;&gt; &lt;img alt=&#34;Super Productivity on Mastodon&#34; src=&#34;https://img.shields.io/badge/-MASTODON-%232B90D9?style=for-the-badge&amp;amp;logo=mastodon&amp;amp;logoColor=white&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://twitter.com/intent/tweet?text=Super%20Productivity%20%20https%3A%2F%2Fsuper-productivity.com&#34;&gt; &lt;img alt=&#34;tweet&#34; src=&#34;https://img.shields.io/twitter/url/https/github.com/ArmynC/ArminC-AutoExec.svg?style=flat-square&amp;amp;logo=twitter&#34; target=&#34;_blank&#34; align=&#34;center&#34;&gt; &lt;/a&gt; &lt;/p&gt; &#xA;&lt;p align=&#34;center&#34; style=&#34;text-align: center;&#34;&gt; &lt;a href=&#34;https://snapcraft.io/superproductivity&#34; target=&#34;_blank&#34;&gt; &lt;img alt=&#34;Get it from the Snap Store&#34; src=&#34;https://snapcraft.io/static/images/badges/en/snap-store-black.svg?sanitize=true&#34; align=&#34;center&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://raw.githubusercontent.com/www.microsoft.com/store/apps/9NHFVG8361TW?cid=storebadge&amp;amp;ocid=badge&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://developer.microsoft.com/store/badges/images/English_get-it-from-MS.png&#34; align=&#34;center&#34; alt=&#34;English badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://raw.githubusercontent.com/apps.apple.com/de/app/super-productivity/id1482572463?l=en&amp;amp;mt=12&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/app-store-badge.svg?sanitize=true&#34; align=&#34;center&#34; alt=&#34;App Store Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://raw.githubusercontent.com/play.google.com/store/apps/details?id=com.superproductivity.superproductivity&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/google-play-badge.png&#34; align=&#34;center&#34; alt=&#34;Play Store Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &lt;a href=&#34;https://raw.githubusercontent.com/f-droid.org/en/packages/com.superproductivity.superproductivity&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://f-droid.org/assets/fdroid-logo-text_S0MUfk_FsnAYL7n2MQye-34IoSNm6QM6xYjDnMqkufo=.svg?sanitize=true&#34; align=&#34;center&#34; alt=&#34;F-Droid Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &lt;/p&gt; &#xA;&lt;br&gt; &#xA;&lt;h2&gt;&lt;span&gt;✔&lt;/span&gt; Features&lt;/h2&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;strong&gt;Keep organized and focused!&lt;/strong&gt; Plan and categorize your tasks using sub-tasks, projects and tags and color code them as needed.&lt;/li&gt; &#xA; &lt;li&gt;Use &lt;strong&gt;timeboxing&lt;/strong&gt; and &lt;strong&gt;track your time&lt;/strong&gt;. Create time sheets and work summaries in a breeze to easily export them to your company&#39;s time tracking system.&lt;/li&gt; &#xA; &lt;li&gt;Helps you to &lt;strong&gt;establish healthy &amp;amp; productive habits&lt;/strong&gt;: &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;A &lt;strong&gt;break reminder&lt;/strong&gt; reminds you when it&#39;s time to step away.&lt;/li&gt; &#xA;   &lt;li&gt;The &lt;strong&gt;anti-procrastination feature&lt;/strong&gt; helps you gain perspective when you really need to.&lt;/li&gt; &#xA;   &lt;li&gt;Need some extra focus? A &lt;strong&gt;Pomodoro timer&lt;/strong&gt; is also always at hand.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Collect personal metrics&lt;/strong&gt; to see, which of your work routines need adjustments.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;Integrate with &lt;strong&gt;Jira&lt;/strong&gt;, &lt;strong&gt;GitHub&lt;/strong&gt;, &lt;strong&gt;GitLab&lt;/strong&gt;, &lt;strong&gt;Gitea&lt;/strong&gt; and &lt;strong&gt;OpenProject&lt;/strong&gt;. Auto import tasks assigned to you, plan the details locally, automatically create work logs, and get notified immediately, when something changes.&lt;/li&gt; &#xA; &lt;li&gt;Attach context information to tasks and projects. Create &lt;strong&gt;notes&lt;/strong&gt;, attach &lt;strong&gt;files&lt;/strong&gt; or create &lt;strong&gt;project-level bookmarks&lt;/strong&gt; for links, files, and even commands.&lt;/li&gt; &#xA; &lt;li&gt;Super Productivity &lt;strong&gt;respects your privacy&lt;/strong&gt; and &lt;strong&gt;does NOT collect any data&lt;/strong&gt; and there are no user accounts or registration. &lt;strong&gt;You decide where you store your data!&lt;/strong&gt;&lt;/li&gt; &#xA; &lt;li&gt;It&#39;s &lt;strong&gt;free&lt;/strong&gt; and &lt;strong&gt;open source&lt;/strong&gt; and always will be.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;And much more!&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://youtu.be/_mvMXWLsL-8&#34;&gt;&lt;img alt=&#34;Work View with global links&#34; src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/screen_standard.png&#34; width=&#34;100%&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;h2&gt;&lt;span&gt;❓&lt;/span&gt; How to use it&lt;/h2&gt; &#xA;&lt;p&gt;If you need some help, &lt;a href=&#34;https://dev.to/johannesjo/getting-started-with-super-productivity-2791&#34;&gt;this article on dev.to is the best place to start&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;If you prefer, there is also a (long) &lt;a href=&#34;https://www.youtube.com/watch?v=VoF2_RSdNXA&#34;&gt;YouTube video available&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;There is another older – the app looks and feels much better now ;) – &lt;a href=&#34;https://dev.to/johannesjo/super-productivity-how-to-grow-fond-of-time-tracking-and-task-management-22ee&#34;&gt;article&lt;/a&gt; on how I personally might use the app - and still &lt;a href=&#34;https://dev.to/johannesjo/the-prioritising-scheme-how-to-eat-the-frog-with-super-productivity-mlk&#34;&gt;another one&lt;/a&gt; on how I implement the &#39;eat the frog&#39; prioritizing scheme in the app.&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/discussions&#34;&gt;If you have further questions, please refer to the discussions page&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;details&gt; &#xA; &lt;summary&gt; &lt;b&gt;⌨ Keyboard shortcuts and short-syntax&lt;/b&gt;&lt;/summary&gt; &#xA; &lt;h3&gt;Keyboard shortcuts (most important)&lt;/h3&gt; &#xA; &lt;ul&gt; &#xA;  &lt;li&gt;&lt;code&gt;Shift&lt;/code&gt;+&lt;code&gt;A&lt;/code&gt;: Open add task bar&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Enter&lt;/code&gt;: Edit currently focussed task title&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Arrow keys&lt;/code&gt;: Navigate around task list&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;D&lt;/code&gt;: Mark currently focused task as done&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;A&lt;/code&gt;: Add subtask to currently focused task&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Y&lt;/code&gt;: Toggle tracking time to currently focused task&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;S&lt;/code&gt;: Open schedule dialog for currently focused task&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Backspace&lt;/code&gt;: Delete currently focused task&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;ArrowRight&lt;/code&gt;: Open additional info panel for currently focused task&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Ctrl&lt;/code&gt;+&lt;code&gt;Shift&lt;/code&gt;+&lt;code&gt;ArrowUp&lt;/code&gt;: Move currently focused task up in list&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Ctrl&lt;/code&gt;+&lt;code&gt;Shift&lt;/code&gt;+&lt;code&gt;ArrowDown&lt;/code&gt;: Move currently focused task down in list&lt;/li&gt; &#xA; &lt;/ul&gt; &#xA; &lt;h3&gt;Short-Syntax&lt;/h3&gt; &#xA; &lt;p&gt;Can be used when adding a task.&lt;/p&gt; &#xA; &lt;ul&gt; &#xA;  &lt;li&gt;&lt;code&gt;# &amp;lt;tag-name&amp;gt;&lt;/code&gt;: add a tag to the task&lt;br&gt; (&lt;code&gt;&#34;task-description #tag1&#34;&lt;/code&gt;)&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;&amp;lt;number&amp;gt;m&lt;/code&gt; or &lt;code&gt;&amp;lt;number&amp;gt;h&lt;/code&gt;: set time-estimate for the task&lt;br&gt; (&lt;code&gt;&#34;task-description 10m&#34;&lt;/code&gt; or &lt;code&gt;&#34;task-description 5h&#34;&lt;/code&gt;)&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;+ &amp;lt;project-name&amp;gt;&lt;/code&gt;: add the task to an existing project&lt;br&gt; (&lt;code&gt;&#34;task-description +Important Project&#34;&lt;/code&gt;)&lt;/li&gt; &#xA;  &lt;li&gt;&lt;code&gt;Ctr + 2&lt;/code&gt;: toggle between moving the new task to the bottom and top of the list&lt;/li&gt; &#xA; &lt;/ul&gt; &#xA;&lt;/details&gt; &#xA;&lt;h2&gt;&lt;span&gt;🌐&lt;/span&gt; Web Version&lt;/h2&gt; &#xA;&lt;p&gt;Check out the web version even though it is a bit limited: Time tracking only works if the app is open and for idle time tracking to work, the chrome extension must be installed.&lt;/p&gt; &#xA;&lt;p&gt;If you want the Jira integration and idle time tracking to work, you also have to download and install the &lt;a href=&#34;https://chrome.google.com/webstore/detail/super-productivity/ljkbjodfmekklcoibdnhahlaalhihmlb&#34;&gt;Super Productivity Chrome Extension&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;&lt;span&gt;💻&lt;/span&gt; Downloads &amp;amp; Install&lt;/h2&gt; &#xA;&lt;h3&gt;All Platforms&lt;/h3&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/releases&#34;&gt;Install from the releases page&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h3&gt;Windows&lt;/h3&gt; &#xA;&lt;p&gt;Due to certification issues it&#39;s recommended to download the app from the Microsoft Store:&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://raw.githubusercontent.com/www.microsoft.com/store/apps/9nhfvg8361tw?cid=storebadge&amp;amp;ocid=badge&#34;&gt;&lt;img src=&#34;https://developer.microsoft.com/store/badges/images/English_get-it-from-MS.png&#34; alt=&#34;English badge&#34; width=&#34;127&#34; height=&#34;52&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;You can also install the app using &lt;a href=&#34;https://community.chocolatey.org/packages/super-productivity&#34;&gt;Chocolatey&lt;/a&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-powershell&#34;&gt;choco install super-productivity&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Linux&lt;/h3&gt; &#xA;&lt;h4&gt;Snap - Most distributions&lt;/h4&gt; &#xA;&lt;p&gt;Install via command-line:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;# stable&#xA;sudo snap install superproductivity&#xA;&#xA;# edge channel releases&#xA;sudo snap install --channel=edge superproductivity&#xA;&#xA;# it is also recommended to disable updates to the app while it is running:&#xA;sudo snap set core experimental.refresh-app-awareness=true&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;a href=&#34;https://snapcraft.io/superproductivity&#34;&gt; &lt;img alt=&#34;Get it from the Snap Store&#34; src=&#34;https://snapcraft.io/static/images/badges/en/snap-store-black.svg?sanitize=true&#34;&gt; &lt;/a&gt; &#xA;&lt;h4&gt;Aur - Arch Linux&lt;/h4&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;git clone https://aur.archlinux.org/superproductivity-bin.git&#xA;cd superproductivity-bin&#xA;makepkg -si&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;MacOS&lt;/h3&gt; &#xA;&lt;p&gt;Install via &lt;a href=&#34;https://github.com/caskroom/homebrew-cask&#34;&gt;homebrew cask&lt;/a&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;brew install --cask superproductivity&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;a href=&#34;https://raw.githubusercontent.com/apps.apple.com/de/app/super-productivity/id1482572463?l=en&amp;amp;mt=12&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/app-store-badge.svg?sanitize=true&#34; alt=&#34;App Store Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &#xA;&lt;h3&gt;Android&lt;/h3&gt; &#xA;&lt;p&gt;There is a &lt;a href=&#34;https://play.google.com/store/apps/details?id=com.superproductivity.superproductivity&amp;amp;hl=gsw&#34;&gt;very early(!) Android version available&lt;/a&gt;. The sources can be &lt;a href=&#34;https://github.com/johannesjo/super-productivity-android&#34;&gt;found here&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;a href=&#34;https://raw.githubusercontent.com/play.google.com/store/apps/details?id=com.superproductivity.superproductivity&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/screens/google-play-badge.png&#34; align=&#34;center&#34; alt=&#34;App Store Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &#xA;&lt;a href=&#34;https://raw.githubusercontent.com/f-droid.org/en/packages/com.superproductivity.superproductivity&#34; target=&#34;_blank&#34;&gt; &lt;img src=&#34;https://f-droid.org/assets/fdroid-logo-text_S0MUfk_FsnAYL7n2MQye-34IoSNm6QM6xYjDnMqkufo=.svg?sanitize=true&#34; align=&#34;center&#34; alt=&#34;F-Droid Badge&#34; height=&#34;50&#34;&gt; &lt;/a&gt; &#xA;&lt;h2&gt;&lt;span&gt;♥&lt;/span&gt; Contributing&lt;/h2&gt; &#xA;&lt;p&gt;Please check out the &lt;a href=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/CONTRIBUTING.md&#34;&gt;CONTRIBUTING.md&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;There are several ways to help.&lt;/p&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;Spread the word:&lt;/strong&gt; More users mean more people testing and contributing to the app which in turn means better stability and possibly more and better features. You can vote for Super Productivity on &lt;a href=&#34;https://www.slant.co/topics/14021/viewpoints/7/~productivity-tools-for-linux~super-productivity&#34;&gt;Slant&lt;/a&gt;, &lt;a href=&#34;https://www.producthunt.com/posts/super-productivity&#34;&gt;Product Hunt&lt;/a&gt;, &lt;a href=&#34;https://www.softpedia.com/get/Office-tools/Diary-Organizers-Calendar/Super-Productivity.shtml&#34;&gt;Softpedia&lt;/a&gt; or on &lt;a href=&#34;https://alternativeto.net/software/super-productivity/&#34;&gt;AlternativeTo&lt;/a&gt;, you can &lt;a href=&#34;https://twitter.com/intent/tweet?text=I%20like%20Super%20Productivity%20%20https%3A%2F%2Fsuper-productivity.com&#34;&gt;tweet about it&lt;/a&gt;, share it on &lt;a href=&#34;http://www.linkedin.com/shareArticle?mini=true&amp;amp;url=https://super-productivity.com&amp;amp;title=I%20like%20Super%20Productivity&amp;amp;&#34;&gt;LinkedIn&lt;/a&gt;, &lt;a href=&#34;http://www.reddit.com/submit?url=https%3A%2F%2Fsuper-productivity.com&amp;amp;title=I%20like%20Super%20Productivity&#34;&gt;reddit&lt;/a&gt; or any of your favorite social media platforms. Every little bit helps!&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;Provide a Pull Request:&lt;/strong&gt; Here is a list of &lt;a href=&#34;https://github.com/johannesjo/super-productivity/issues?q=is%3Aissue+is%3Aopen+sort%3Areactions-%2B1-desc&#34;&gt;the most popular community requests&lt;/a&gt; and here some info on &lt;a href=&#34;https://github.com/johannesjo/super-productivity#running-the-development-server&#34;&gt;how to run the development build&lt;/a&gt;. Please make sure that you&#39;re following the &lt;a href=&#34;https://github.com/angular/angular.js/raw/master/DEVELOPERS.md#commits&#34;&gt;angular commit guidelines&lt;/a&gt; and to also include the issue number in your commit message, if you&#39;re fixing a particular issue (e.g.: &lt;code&gt;feat: add nice feature with the number #31&lt;/code&gt;).&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/discussions&#34;&gt;Answer questions&lt;/a&gt;&lt;/strong&gt;: You know the answer to another user&#39;s problem? Share your knowledge!&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/issues?q=is%3Aissue+is%3Aopen+sort%3Areactions-%2B1-desc+label%3A%22community+feedback+wanted%22&#34;&gt;Provide your opinion&lt;/a&gt;:&lt;/strong&gt; Some community suggestions are controversial. Your input might be helpful and if it is just an up- or down-vote.&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/issues?q=is%3Aissue+is%3Aopen+label%3A%22needs+concept+and%2For+ui+spec%22&#34;&gt;Provide a more refined UI spec for existing feature requests&lt;/a&gt;&lt;/strong&gt;&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/issues/new&#34;&gt;Report bugs&lt;/a&gt;&lt;/strong&gt;&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/issues/new&#34;&gt;Make a feature or improvement request&lt;/a&gt;&lt;/strong&gt;: Something can be done better? Something essential missing? Let us know!&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/johannesjo/super-productivity/tree/master/src/assets/i18n&#34;&gt;Translations&lt;/a&gt;, Icons, etc.&lt;/strong&gt;: You don&#39;t have to be programmer to help. Some of the icons really need improvement and many of the translations could use some love.&lt;/p&gt; &lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://fink.inlang.com/github.com/johannesjo/super-productivity?ref=badge&#34;&gt;&lt;img src=&#34;https://badge.inlang.com/?url=github.com/johannesjo/super-productivity&#34; alt=&#34;inlang status badge&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;You can use the Fink Localization Editor to edit, lint, and add translations for different languages. &lt;a href=&#34;https://inlang.com/g/6ddyhpoi&#34;&gt;Contribute via fink Guide&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;ol start=&#34;9&#34;&gt; &#xA; &lt;li&gt;&lt;strong&gt;&lt;a href=&#34;https://github.com/sponsors/johannesjo&#34;&gt;Sponsor the project&lt;/a&gt;&lt;/strong&gt;&lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;h3&gt;Running the development server&lt;/h3&gt; &#xA;&lt;p&gt;To run the development server you need to have node installed at least in the version 14. Go to &lt;a href=&#34;https://nodejs.org&#34;&gt;https://nodejs.org&lt;/a&gt; for installation instructions.&lt;/p&gt; &#xA;&lt;p&gt;&lt;strong&gt;Clone repo&lt;/strong&gt;&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;git clone https://github.com/johannesjo/super-productivity.git&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;&lt;strong&gt;Install dependencies&lt;/strong&gt;&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;cd super-productivity&#xA;npm i -g @angular/cli&#xA;npm i&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;&lt;strong&gt;Run the dev server&lt;/strong&gt;&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;ng serve&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Afterwards you can open &lt;a href=&#34;http://localhost:4200&#34;&gt;http://localhost:4200&lt;/a&gt; in your browser. For most adjustments this should be enough and also enables you to use the &lt;a href=&#34;https://chrome.google.com/webstore/detail/redux-devtools/lmhkpmbekcpmknklioeibfkpmmfibljd?hl=de&#34;&gt;redux dev tools&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;If you want to run the app standalone do the following:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;ng serve&#xA;&#xA;# in a new console tab&#xA;npm start&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Packaging the app&lt;/h3&gt; &#xA;&lt;p&gt;Packaging the app is done via &lt;a href=&#34;https://github.com/electron-userland/electron-builder&#34;&gt;electron-builder&lt;/a&gt;. To start packaging run &lt;code&gt;npm run dist&lt;/code&gt;. If you want to add new platforms and experiment with the build options the easiest way to do so is manipulating the &lt;code&gt;build&lt;/code&gt; property in the &lt;a href=&#34;https://github.com/johannesjo/super-productivity/raw/develop/package.json&#34;&gt;package.json&lt;/a&gt;, but you can also use the &lt;a href=&#34;https://www.electron.build/cli&#34;&gt;command line interface of electron builder&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Run as Docker Container&lt;/h2&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;docker run -d -p 80:80 johannesjo/super-productivity:latest&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Custom themes (desktop only)&lt;/h2&gt; &#xA;&lt;p&gt;In addition to color coding your projects and tags and to the dark and light theme you can also load completely custom css to restyle everything. To load a custom theme you simply need put them into a new file named &lt;code&gt;styles.css&lt;/code&gt; directly in the &lt;a href=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/#user-data-folder&#34;&gt;user data folder&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;There is a great set of &lt;a href=&#34;https://github.com/johannesjo/super-productivity-themes/tree/main/dist&#34;&gt;themes available for download in this repository&lt;/a&gt; as well as some &lt;a href=&#34;https://github.com/johannesjo/super-productivity-themes&#34;&gt;info on how to create your own custom themes&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Custom WebDAV Syncing&lt;/h2&gt; &#xA;&lt;p&gt;If using the integrated WebDAV Syncing is not working out for you, you can also try to sync all app files, which are located in the &lt;a href=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/#user-data-folder&#34;&gt;user data folder&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Automatic Backups&lt;/h2&gt; &#xA;&lt;p&gt;For the desktop version automatic backups are stored per default in the &lt;code&gt;backup&lt;/code&gt; sub folder of the &lt;a href=&#34;https://raw.githubusercontent.com/johannesjo/super-productivity/master/#user-data-folder&#34;&gt;user data folder&lt;/a&gt;. Once you located a backup you can restore it on the settings page under &lt;code&gt;Sync &amp;amp; Export/Import/Export/Import from file&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;User Data Folder&lt;/h2&gt; &#xA;&lt;p&gt;Where user data is stored differs from OS to OS. The most common locations are:&lt;/p&gt; &#xA;&lt;table&gt; &#xA; &lt;thead&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;th&gt;OS&lt;/th&gt; &#xA;   &lt;th&gt;Location&lt;/th&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/thead&gt; &#xA; &lt;tbody&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;&lt;strong&gt;Mac OS&lt;/strong&gt;&lt;/td&gt; &#xA;   &lt;td&gt;&lt;code&gt;~/Library/Application Support/superProductivity/&lt;/code&gt;&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;&lt;strong&gt;Windows&lt;/strong&gt;&lt;/td&gt; &#xA;   &lt;td&gt;&lt;code&gt;C:\Users\&amp;lt;YOUR_USER_NAME&amp;gt;\AppData\Roaming\superProductivity/&lt;/code&gt; or &lt;code&gt;%APPDATA%\superProductivity&lt;/code&gt;&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;&lt;strong&gt;Linux&lt;/strong&gt;&lt;/td&gt; &#xA;   &lt;td&gt;&lt;code&gt;~/.config/superProductivity/&lt;/code&gt;&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/tbody&gt; &#xA;&lt;/table&gt; &#xA;&lt;p&gt;The path should be shown when you go to the &#34;Automatic Backups` section on the configuration page (minus the &#34;backup&#34; sub folder). You can also see it printed out when you start the app from the command line.&lt;/p&gt; &#xA;&lt;p&gt;For web browsers, data is simply stored for the current user inside the browser storage.&lt;/p&gt; &#xA;&lt;h3&gt;Using a custom folder to save user data&lt;/h3&gt; &#xA;&lt;p&gt;You can specify a custom folder for saving your data by starting the application with the &lt;code&gt;--user-data-dir&lt;/code&gt; parameter like so:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;superproductivity --user-data-dir=/path/to/my/data&#xA;&lt;/code&gt;&lt;/pre&gt;</summary>
  </entry>
  <entry>
    <title>solana-labs/solana-web3.js</title>
    <updated>2024-08-04T01:45:04Z</updated>
    <id>tag:github.com,2024-08-04:/solana-labs/solana-web3.js</id>
    <link href="https://github.com/solana-labs/solana-web3.js" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Solana JavaScript SDK&lt;/p&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&#34;https://www.npmjs.com/package/@solana/web3.js/v/rc&#34;&gt;&lt;img src=&#34;https://img.shields.io/npm/v/@solana/web3.js/rc.svg?style=flat&#34; alt=&#34;npm&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://www.npmjs.com/package/@solana/web3.js/v/rc&#34;&gt;&lt;img src=&#34;https://img.shields.io/npm/dm/@solana/web3.js/rc.svg?style=flat&#34; alt=&#34;npm-downloads&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://github.com/semantic-release/semantic-release&#34;&gt;&lt;img src=&#34;https://img.shields.io/badge/%20%20%F0%9F%93%A6%F0%9F%9A%80-semantic--release-e10079.svg?sanitize=true&#34; alt=&#34;semantic-release&#34;&gt;&lt;/a&gt; &lt;br&gt; &lt;a href=&#34;https://github.com/prettier/prettier&#34;&gt;&lt;img src=&#34;https://img.shields.io/badge/code_style-prettier-ff69b4.svg?style=flat-square&#34; alt=&#34;code-style-prettier&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;h1&gt;Solana JavaScript SDK&lt;/h1&gt; &#xA;&lt;p&gt;This is the JavaScript SDK for building Solana apps for Node, web, and React Native.&lt;/p&gt; &#xA;&lt;h1&gt;Installation&lt;/h1&gt; &#xA;&lt;p&gt;For use in a Node.js or web application:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;npm install --save @solana/web3.js@rc&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;For use in a browser, without a build system:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-html&#34;&gt;&amp;lt;!-- Development (debug mode, unminified) --&amp;gt;&#xA;&amp;lt;script src=&#34;https://unpkg.com/@solana/web3.js@rc/dist/index.development.js&#34;&amp;gt;&amp;lt;/script&amp;gt;&#xA;&#xA;&amp;lt;!-- Production (minified) --&amp;gt;&#xA;&amp;lt;script src=&#34;https://unpkg.com/@solana/web3.js@rc/dist/index.production.min.js&#34;&amp;gt;&amp;lt;/script&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;Examples&lt;/h1&gt; &#xA;&lt;p&gt;To get a feel for the API, run and modify the live examples in the &lt;code&gt;examples/&lt;/code&gt; directory. There, you will find a series of single-purpose Node scripts that demonstrate a specific feature or use case. You will also find a React application that you can run in a browser, that demonstrates being able to create, sign, and send transactions using browser wallets.&lt;/p&gt; &#xA;&lt;h1&gt;What&#39;s New in Version 2.0&lt;/h1&gt; &#xA;&lt;p&gt;Version 2.0 of the Solana JavaScript SDK is a response to many of the pain points you have communicated to us when developing Solana applications with web3.js.&lt;/p&gt; &#xA;&lt;h2&gt;Tree-Shakability&lt;/h2&gt; &#xA;&lt;p&gt;The object-oriented design of the web3.js (1.x) API prevents optimizing compilers from being able to ‘tree-shake’ unused code from your production builds. No matter how much of the web3.js API you use in your application, you have until now been forced to package all of it.&lt;/p&gt; &#xA;&lt;p&gt;Read more about tree-shaking here:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Glossary/Tree_shaking&#34;&gt;Mozilla Developer Docs: Tree Shaking&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://webpack.js.org/guides/tree-shaking/&#34;&gt;WebPack Docs: Tree Shaking&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://web.dev/articles/reduce-javascript-payloads-with-tree-shaking&#34;&gt;Web.Dev Blog Article: Reduce JavaScript Payloads with Tree Shaking&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;One example of an API that can’t be tree-shaken is the &lt;code&gt;Connection&lt;/code&gt; class. It has dozens of methods, but because it’s a &lt;em&gt;class&lt;/em&gt; you have no choice but to include every method in your application’s final bundle, no matter how many you &lt;em&gt;actually&lt;/em&gt; use.&lt;/p&gt; &#xA;&lt;p&gt;Needlessly large JavaScript bundles can cause issues with deployments to cloud compute providers like Cloudflare or AWS Lambda. They also impact webapp startup performance because of longer download and JavaScript parse times.&lt;/p&gt; &#xA;&lt;p&gt;Version 2.0 is fully tree-shakable and will remain so, enforced by build-time checks. Optimizing compilers can now eliminate those parts of the library that your application does not use.&lt;/p&gt; &#xA;&lt;p&gt;The new library itself is comprised of several smaller, modular packages under the &lt;code&gt;@solana&lt;/code&gt; organization, including:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/accounts&lt;/code&gt;: For fetching and decoding accounts&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/codecs&lt;/code&gt;: For composing data (de)serializers from a set of primitives or building custom ones&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/errors&lt;/code&gt;: For identifying and refining coded errors thrown in the &lt;code&gt;@solana&lt;/code&gt; namespace&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc&lt;/code&gt;: For sending RPC requests&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;: For subscribing to RPC notifications&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/signers&lt;/code&gt;: For building message and/or transaction signer objects&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/sysvars&lt;/code&gt;: For fetching and decoding sysvar accounts&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/transaction-messages&lt;/code&gt;: For building and transforming Solana transaction message objects&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/transactions&lt;/code&gt;: For compiling and signing transactions for submission to the network&lt;/li&gt; &#xA; &lt;li&gt;And many more!&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Some of these packages are themselves composed of smaller packages. For instance, &lt;code&gt;@solana/rpc&lt;/code&gt; is composed of &lt;code&gt;@solana/rpc-spec&lt;/code&gt; (for core JSON RPC specification types), &lt;code&gt;@solana/rpc-api&lt;/code&gt; (for the Solana-specific RPC methods), &lt;code&gt;@solana/rpc-transport-http&lt;/code&gt; (for the default HTTP transport) and so on.&lt;/p&gt; &#xA;&lt;p&gt;Developers can use the default configurations within the main library (&lt;code&gt;@solana/web3.js@rc&lt;/code&gt;) or import any of its subpackages where customization-through-composition is desired.&lt;/p&gt; &#xA;&lt;h2&gt;Composable Internals&lt;/h2&gt; &#xA;&lt;p&gt;Depending on your use case and your tolerance for certain application behaviours, you may wish to configure your application to make a different set of tradeoffs than another developer. The web3.js (1.x) API imposed a rigid set of common-case defaults on &lt;em&gt;all&lt;/em&gt; developers, some of which were impossible to change.&lt;/p&gt; &#xA;&lt;p&gt;The inability to customize web3.js up until now has been a source of frustration:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;The Mango team wanted to customize the transaction confirmation strategy, but all of that functionality is hidden away behind &lt;code&gt;confirmTransaction&lt;/code&gt; – a static method of &lt;code&gt;Connection&lt;/code&gt;. &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/raw/69a8ad25ef09f9e6d5bff1ffa8428d9be0bd32ac/packages/library-legacy/src/connection.ts#L3734&#34;&gt;Here’s the code for &lt;code&gt;confirmTransaction&lt;/code&gt; on GitHub&lt;/a&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Solana developer ‘mPaella’ &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/issues/1143#issuecomment-1435927152&#34;&gt;wanted us to add a feature in the RPC&lt;/a&gt; that would failover to a set of backup URLs in case the primary one failed.&lt;/li&gt; &#xA; &lt;li&gt;Solana developer ‘epicfaace’ wanted first-class support for automatic time-windowed batching in the RPC transport. &lt;a href=&#34;https://github.com/solana-labs/solana/pull/23628&#34;&gt;Here’s their pull request&lt;/a&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Multiple folks have expressed the need for custom retry logic for failed requests or transactions. &lt;a href=&#34;https://github.com/solana-labs/solana/pull/11811&#34;&gt;Here’s a pull request from ‘dafyddd’&lt;/a&gt; and &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/issues/1041&#34;&gt;another from ‘abrkn’&lt;/a&gt; attempting to modify retry logic to suit their individual use cases.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Version 2.0 exposes far more of its internals, particularly where communication with an RPC is concerned, and allows willing developers the ability to compose new implementations from the default ones that manifest a nearly limitless array of customizations.&lt;/p&gt; &#xA;&lt;p&gt;The individual modules that make up web3.js are assembled in a &lt;strong&gt;default&lt;/strong&gt; configuration reminiscent of the legacy library as part of the npm package &lt;code&gt;@solana/web3.js@rc&lt;/code&gt;, but those who wish to assemble them in different configurations may do so.&lt;/p&gt; &#xA;&lt;p&gt;Generic types are offered in numerous places, allowing you to specify new functionality, to make extensions to each API via composition and supertypes, and to encourage you to create higher-level opinionated abstractions of your own.&lt;/p&gt; &#xA;&lt;p&gt;In fact, we expect you to do so, and to open source some of those for use by others with similar needs.&lt;/p&gt; &#xA;&lt;h2&gt;Modern JavaScript; Zero-Dependency&lt;/h2&gt; &#xA;&lt;p&gt;The advance of modern JavaScript features presents an opportunity to developers of crypto applications, such as the ability to use native Ed25519 keys and to express large values as native &lt;code&gt;bigint&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;The Web Incubator Community Group has advocated for the addition of Ed25519 support to the &lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API&#34;&gt;Web Crypto API&lt;/a&gt;, and support has already landed in &lt;em&gt;most&lt;/em&gt; modern JavaScript runtimes.&lt;/p&gt; &#xA;&lt;p&gt;Engine support for &lt;code&gt;bigint&lt;/code&gt; values has also become commonplace. The older &lt;code&gt;number&lt;/code&gt; primitive in JavaScript has a maximum value of 2^53 - 1, whereas Rust’s &lt;code&gt;u64&lt;/code&gt; can represent values up to 2^64.&lt;/p&gt; &#xA;&lt;p&gt;Version 2.0 eliminates userspace implementations of Ed25519 cryptography, large number polyfills, and more, in favour of custom implementations or the use of native JavaScript features, reducing the size of the library. It has no third-party dependencies.&lt;/p&gt; &#xA;&lt;h2&gt;Functional Architecture&lt;/h2&gt; &#xA;&lt;p&gt;The object oriented, class-based architecture of web3.js (1.x) causes unnecessary bundle bloat. Your application has no choice but to bundle &lt;em&gt;all&lt;/em&gt; of the functionality and dependencies of a class no matter how many methods you actually use at runtime.&lt;/p&gt; &#xA;&lt;p&gt;Class-based architecture also presents unique risks to developers who trigger the dual-package hazard. This describes a situation you can find yourself in if you build for both CommonJS and ES modules. It arises when two copies of the same class are present in the dependency tree, causing checks like &lt;code&gt;instanceof&lt;/code&gt; to fail. This introduces aggravating and difficult to debug problems.&lt;/p&gt; &#xA;&lt;p&gt;Read more about dual-package hazard:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://nodejs.org/api/packages.html#dual-package-hazard&#34;&gt;NodeJS: Dual Package Hazard&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Version 2.0 implements no classes (with the notable exception of the &lt;code&gt;SolanaError&lt;/code&gt; class) and implements the thinnest possible interfaces at function boundaries.&lt;/p&gt; &#xA;&lt;h2&gt;Statistics&lt;/h2&gt; &#xA;&lt;p&gt;Consider these statistical comparisons between version 2.0 and the legacy 1.x.&lt;/p&gt; &#xA;&lt;table&gt; &#xA; &lt;thead&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;th&gt;&lt;/th&gt; &#xA;   &lt;th&gt;1.x (Legacy)&lt;/th&gt; &#xA;   &lt;th&gt;2.0&lt;/th&gt; &#xA;   &lt;th&gt;+/- %&lt;/th&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/thead&gt; &#xA; &lt;tbody&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Total minified size of library&lt;/td&gt; &#xA;   &lt;td&gt;81 KB&lt;/td&gt; &#xA;   &lt;td&gt;57.5 KB&lt;/td&gt; &#xA;   &lt;td&gt;-29%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Total minified size of library (when runtime supports Ed25519)&lt;/td&gt; &#xA;   &lt;td&gt;81 KB&lt;/td&gt; &#xA;   &lt;td&gt;53 KB&lt;/td&gt; &#xA;   &lt;td&gt;-33%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Bundled size of a web application that executes a transfer of lamports&lt;/td&gt; &#xA;   &lt;td&gt;111 KB&lt;/td&gt; &#xA;   &lt;td&gt;23.9 KB&lt;/td&gt; &#xA;   &lt;td&gt;-78%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Bundled size of a web application that executes a transfer of lamports (when runtime supports Ed25519)&lt;/td&gt; &#xA;   &lt;td&gt;111 KB&lt;/td&gt; &#xA;   &lt;td&gt;18.2 KB&lt;/td&gt; &#xA;   &lt;td&gt;-83%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Performance of key generation, signing, and verifying signatures (Brave with Experimental API flag)&lt;/td&gt; &#xA;   &lt;td&gt;700 ops/s&lt;/td&gt; &#xA;   &lt;td&gt;7000 ops/s&lt;/td&gt; &#xA;   &lt;td&gt;+900%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;First-load size for Solana Explorer&lt;/td&gt; &#xA;   &lt;td&gt;311 KB&lt;/td&gt; &#xA;   &lt;td&gt;228 KB&lt;/td&gt; &#xA;   &lt;td&gt;-26%&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/tbody&gt; &#xA;&lt;/table&gt; &#xA;&lt;p&gt;The re-engineered library achieves these speedups and reductions in bundle size in large part through use of modern JavaScript APIs.&lt;/p&gt; &#xA;&lt;p&gt;To validate our work, we replaced the legacy 1.x library with the new 2.0 library on the homepage of the Solana Explorer. Total first-load bundle size dropped by 26% without removing a single feature. &lt;a href=&#34;https://twitter.com/callum_codes/status/1679124485218226176&#34;&gt;Here’s an X thread&lt;/a&gt; by Callum McIntyre if you would like to dig deeper.&lt;/p&gt; &#xA;&lt;h1&gt;A Tour of the Version 2.0 API&lt;/h1&gt; &#xA;&lt;p&gt;Here’s an overview of how to use the new library to interact with the RPC, configure network transports, work with Ed25519 keys, and to serialize data.&lt;/p&gt; &#xA;&lt;h2&gt;RPC&lt;/h2&gt; &#xA;&lt;p&gt;Version 2.0 ships with an implementation of the &lt;a href=&#34;https://www.jsonrpc.org/specification&#34;&gt;JSON RPC specification&lt;/a&gt; and a type spec for the &lt;a href=&#34;https://docs.solana.com/api&#34;&gt;Solana JSON RPC&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;The main package responsible for managing communication with an RPC is &lt;code&gt;@solana/rpc&lt;/code&gt;. However, this package makes use of more granular packages to break down the RPC logic into smaller pieces. Namely, these packages are:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc&lt;/code&gt;: Contains all logic related to sending Solana RPC calls.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-api&lt;/code&gt;: Describes all Solana RPC methods using types.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-transport-http&lt;/code&gt;: Provides a concrete implementation of an RPC transport using HTTP requests.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-spec&lt;/code&gt;: Defines the JSON RPC spec for sending RPC requests.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-spec-types&lt;/code&gt;: Shared JSON RPC specifications types and helpers that are used by both &lt;code&gt;@solana/rpc&lt;/code&gt; and &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt; (described in the next section).&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-types&lt;/code&gt;: Shared Solana RPC types and helpers that are used by both &lt;code&gt;@solana/rpc&lt;/code&gt; and &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;The main &lt;code&gt;@solana/web3.js&lt;/code&gt; package re-exports the &lt;code&gt;@solana/rpc&lt;/code&gt; package so, going forward, we will import RPC types and functions from the library directly.&lt;/p&gt; &#xA;&lt;h3&gt;RPC Calls&lt;/h3&gt; &#xA;&lt;p&gt;You can use the &lt;code&gt;createSolanaRpc&lt;/code&gt; function by providing the URL of a Solana JSON RPC server. This will create a default client for interacting with the Solana JSON RPC API.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpc } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create an RPC client.&#xA;const rpc = createSolanaRpc(&#39;http://127.0.0.1:8899&#39;);&#xA;//    ^? Rpc&amp;lt;SolanaRpcApi&amp;gt;&#xA;&#xA;// Send a request.&#xA;const slot = await rpc.getSlot().send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Custom RPC Transports&lt;/h3&gt; &#xA;&lt;p&gt;The &lt;code&gt;createSolanaRpc&lt;/code&gt; function communicates with the RPC server using a default HTTP transport that should satisfy most use cases. You can provide your own transport or wrap an existing one to communicate with RPC servers in any way you see fit. In the example below, we explicitly create a transport and use it to create a new RPC client via the &lt;code&gt;createSolanaRpcFromTransport&lt;/code&gt; function.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpcFromTransport, createDefaultRpcTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create an HTTP transport or any custom transport of your choice.&#xA;const transport = createDefaultRpcTransport({ url: &#39;https://api.devnet.solana.com&#39; });&#xA;&#xA;// Create an RPC client using that transport.&#xA;const rpc = createSolanaRpcFromTransport(transport);&#xA;//    ^? Rpc&amp;lt;SolanaRpcApi&amp;gt;&#xA;&#xA;// Send a request.&#xA;const slot = await rpc.getSlot().send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;A custom transport can implement specialized functionality such as coordinating multiple transports, implementing retries, and more. Let&#39;s take a look at some concrete examples.&lt;/p&gt; &#xA;&lt;h4&gt;Round Robin&lt;/h4&gt; &#xA;&lt;p&gt;A ‘round robin’ transport is one that distributes requests to a list of endpoints in sequence.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createDefaultRpcTransport, createSolanaRpcFromTransport, type RpcTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create an HTTP transport for each RPC server.&#xA;const transports = [&#xA;    createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-1.com&#39; }),&#xA;    createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-2.com&#39; }),&#xA;    createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-3.com&#39; }),&#xA;];&#xA;&#xA;// Set up the round-robin transport.&#xA;let nextTransport = 0;&#xA;async function roundRobinTransport&amp;lt;TResponse&amp;gt;(...args: Parameters&amp;lt;RpcTransport&amp;gt;): Promise&amp;lt;TResponse&amp;gt; {&#xA;    const transport = transports[nextTransport];&#xA;    nextTransport = (nextTransport + 1) % transports.length;&#xA;    return await transport(...args);&#xA;}&#xA;&#xA;// Create an RPC client using the round-robin transport.&#xA;const rpc = createSolanaRpcFromTransport(roundRobinTransport);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h4&gt;Sharding&lt;/h4&gt; &#xA;&lt;p&gt;A sharding transport is a kind of distributing transport that sends requests to a particular server based on something about the request itself. Here’s an example that sends requests to different servers depending on the name of the method:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createDefaultRpcTransport, createSolanaRpcFromTransport, type RpcTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create multiple transports.&#xA;const transportA = createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-1.com&#39; });&#xA;const transportB = createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-2.com&#39; });&#xA;const transportC = createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-3.com&#39; });&#xA;const transportD = createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-4.com&#39; });&#xA;&#xA;// Function to determine which shard to use based on the request method.&#xA;function selectShard(method: string): RpcTransport {&#xA;    switch (method) {&#xA;        case &#39;getAccountInfo&#39;:&#xA;        case &#39;getBalance&#39;:&#xA;            return transportA;&#xA;        case &#39;getTransaction&#39;:&#xA;        case &#39;getRecentBlockhash&#39;:&#xA;            return transportB;&#xA;        case &#39;sendTransaction&#39;:&#xA;            return transportC;&#xA;        default:&#xA;            return transportD;&#xA;    }&#xA;}&#xA;&#xA;// Create a transport that selects the correct transport given the request method name.&#xA;async function shardingTransport&amp;lt;TResponse&amp;gt;(...args: Parameters&amp;lt;RpcTransport&amp;gt;): Promise&amp;lt;TResponse&amp;gt; {&#xA;    const payload = args[0].payload as { method: string };&#xA;    const selectedTransport = selectShard(payload.method);&#xA;    return (await selectedTransport(...args)) as TResponse;&#xA;}&#xA;&#xA;// Create an RPC client using the sharding transport.&#xA;const rpc = createSolanaRpcFromTransport(shardingTransport);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h4&gt;Retry&lt;/h4&gt; &#xA;&lt;p&gt;A custom transport is a good place to implement global retry logic for every request:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createDefaultRpcTransport, createSolanaRpcFromTransport, type RpcTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Set the maximum number of attempts to retry a request.&#xA;const MAX_ATTEMPTS = 4;&#xA;&#xA;// Create the default transport.&#xA;const defaultTransport = createDefaultRpcTransport({ url: &#39;https://mainnet-beta.my-server-1.com&#39; });&#xA;&#xA;// Sleep function to wait for a given number of milliseconds.&#xA;function sleep(ms: number): Promise&amp;lt;void&amp;gt; {&#xA;    return new Promise(resolve =&amp;gt; setTimeout(resolve, ms));&#xA;}&#xA;&#xA;// Calculate the delay for a given attempt.&#xA;function calculateRetryDelay(attempt: number): number {&#xA;    // Exponential backoff with a maximum of 1.5 seconds.&#xA;    return Math.min(100 * Math.pow(2, attempt), 1500);&#xA;}&#xA;&#xA;// A retrying transport that will retry up to MAX_ATTEMPTS times before failing.&#xA;async function retryingTransport&amp;lt;TResponse&amp;gt;(...args: Parameters&amp;lt;RpcTransport&amp;gt;): Promise&amp;lt;TResponse&amp;gt; {&#xA;    let requestError;&#xA;    for (let attempts = 0; attempts &amp;lt; MAX_ATTEMPTS; attempts++) {&#xA;        try {&#xA;            return await defaultTransport(...args);&#xA;        } catch (err) {&#xA;            requestError = err;&#xA;            // Only sleep if we have more attempts remaining.&#xA;            if (attempts &amp;lt; MAX_ATTEMPTS - 1) {&#xA;                const retryDelay = calculateRetryDelay(attempts);&#xA;                await sleep(retryDelay);&#xA;            }&#xA;        }&#xA;    }&#xA;    throw requestError;&#xA;}&#xA;&#xA;// Create the RPC client using the retrying transport.&#xA;const rpc = createSolanaRpcFromTransport(retryingTransport);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h4&gt;Failover&lt;/h4&gt; &#xA;&lt;p&gt;Support for handling network failures can be implemented in the transport itself. Here’s an example of some failover logic integrated into a transport:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;// TODO: Your turn; send us a pull request with an example.&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Augmenting/Constraining the RPC API&lt;/h3&gt; &#xA;&lt;p&gt;Using the &lt;code&gt;createSolanaRpc&lt;/code&gt; or &lt;code&gt;createSolanaRpcFromTransport&lt;/code&gt; methods, we always get the same API that includes the Solana RPC API methods. Since the RPC API is described using types only, it is possible to augment those types to add your own methods.&lt;/p&gt; &#xA;&lt;p&gt;When constraining the API scope, keep in mind that types don’t affect bundle size. You may still like to constrain the type-spec for a variety of reasons, including reducing TypeScript noise.&lt;/p&gt; &#xA;&lt;h4&gt;Constraining by Cluster&lt;/h4&gt; &#xA;&lt;p&gt;If you&#39;re using a specific cluster, you may wrap your RPC URL inside a helper function like &lt;code&gt;mainnet&lt;/code&gt; or &lt;code&gt;devnet&lt;/code&gt; to inject that information into the RPC type system.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpc, mainnet, devnet } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const mainnetRpc = createSolanaRpc(mainnet(&#39;https://api.mainnet-beta.solana.com&#39;));&#xA;//    ^? RpcMainnet&amp;lt;SolanaRpcApiMainnet&amp;gt;&#xA;&#xA;const devnetRpc = createSolanaRpc(devnet(&#39;https://api.devnet.solana.com&#39;));&#xA;//    ^? RpcDevnet&amp;lt;SolanaRpcApiDevnet&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;In the example above, &lt;code&gt;devnetRpc.requestAirdrop(..)&lt;/code&gt; will work, but &lt;code&gt;mainnetRpc.requestAirdrop(..)&lt;/code&gt; will raise a TypeScript error since &lt;code&gt;requestAirdrop&lt;/code&gt; is not a valid method of the mainnet cluster.&lt;/p&gt; &#xA;&lt;h4&gt;Cherry-Picking API Methods&lt;/h4&gt; &#xA;&lt;p&gt;You can constrain the API’s type-spec even further so you are left only with the methods you need. The simplest way to do this is to cast the created RPC client to a type that only includes the required methods.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpc, type Rpc, type GetAccountInfoApi, type GetMultipleAccountsApi } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const rpc = createSolanaRpc(&#39;http://127.0.0.1:8899&#39;) as Rpc&amp;lt;GetAccountInfoApi &amp;amp; GetMultipleAccountsApi&amp;gt;;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Alternatively, you can explicitly create the RPC API using the &lt;code&gt;createSolanaRpcApi&lt;/code&gt; function. You will need to create your own transport and bind the two together using the &lt;code&gt;createRpc&lt;/code&gt; function.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import {&#xA;    createDefaultRpcTransport,&#xA;    createRpc,&#xA;    createSolanaRpcApi,&#xA;    DEFAULT_RPC_CONFIG,&#xA;    type GetAccountInfoApi,&#xA;    type GetMultipleAccountsApi,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;const api = createSolanaRpcApi&amp;lt;GetAccountInfoApi &amp;amp; GetMultipleAccountsApi&amp;gt;(DEFAULT_RPC_CONFIG);&#xA;const transport = createDefaultRpcTransport({ url: &#39;http:127.0.0.1:8899&#39; });&#xA;&#xA;const rpc = createRpc({ api, transport });&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Note that the &lt;code&gt;createSolanaRpcApi&lt;/code&gt; function is a wrapper on top of the &lt;code&gt;createRpcApi&lt;/code&gt; function which adds some Solana-specific transformers such as setting a default commitment on all methods or throwing an error when an integer overflow is detected.&lt;/p&gt; &#xA;&lt;h4&gt;Creating Your Own API Methods&lt;/h4&gt; &#xA;&lt;p&gt;The new library’s RPC specification supports an &lt;em&gt;infinite&lt;/em&gt; number of JSON-RPC methods with &lt;strong&gt;zero increase&lt;/strong&gt; in bundle size.&lt;/p&gt; &#xA;&lt;p&gt;This means the library can support future additions to the official &lt;a href=&#34;https://docs.solana.com/api&#34;&gt;Solana JSON RPC&lt;/a&gt;, or &lt;a href=&#34;https://docs.helius.dev/compression-and-das-api/digital-asset-standard-das-api/get-asset&#34;&gt;custom RPC methods&lt;/a&gt; defined by some RPC provider.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of how a developer at might build a custom RPC type-spec for an RPC provider&#39;s implementation of the Metaplex Digital Asset Standard&#39;s &lt;code&gt;getAsset&lt;/code&gt; method:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { RpcApiMethods } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Define the method&#39;s response payload.&#xA;type GetAssetApiResponse = Readonly&amp;lt;{&#xA;    interface: DasApiAssetInterface;&#xA;    id: Address;&#xA;    content: Readonly&amp;lt;{&#xA;        files?: readonly {&#xA;            mime?: string;&#xA;            uri?: string;&#xA;            [key: string]: unknown;&#xA;        }[];&#xA;        json_uri: string;&#xA;        links?: readonly {&#xA;            [key: string]: unknown;&#xA;        }[];&#xA;        metadata: DasApiMetadata;&#xA;    }&amp;gt;;&#xA;    /* ...etc... */&#xA;}&amp;gt;;&#xA;&#xA;// Set up an interface for the request method.&#xA;interface GetAssetApi extends RpcApiMethods {&#xA;    // Define the method&#39;s name, parameters and response type&#xA;    getAsset(args: { id: Address }): GetAssetApiResponse;&#xA;}&#xA;&#xA;// Export the type spec for downstream users.&#xA;export type MetaplexDASApi = GetAssetApi;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Here’s how a developer might use it:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createDefaultRpcTransport, createRpc, createRpcApi } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create the custom API.&#xA;const api = createRpcApi&amp;lt;MetaplexDASApi&amp;gt;();&#xA;&#xA;// Set up an HTTP transport to a server that supports the custom API.&#xA;const transport = createDefaultRpcTransport({&#xA;    url: &#39;https://mainnet.helius-rpc.com/?api-key=&amp;lt;api_key&amp;gt;&#39;,&#xA;});&#xA;&#xA;// Create the RPC client.&#xA;const metaplexDASRpc = createRpc({ api, transport });&#xA;//    ^? Rpc&amp;lt;MetaplexDASApi&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;As long as a particular JSON RPC method adheres to the &lt;a href=&#34;https://www.jsonrpc.org/specification&#34;&gt;official JSON RPC specification&lt;/a&gt;, it will be supported by version 2.0.&lt;/p&gt; &#xA;&lt;h3&gt;Aborting RPC Requests&lt;/h3&gt; &#xA;&lt;p&gt;RPC requests are now abortable with modern &lt;code&gt;AbortControllers&lt;/code&gt;. When calling an RPC method such as &lt;code&gt;getSlot&lt;/code&gt;, it will return a &lt;code&gt;PendingRpcRequest&lt;/code&gt; proxy object that contains a &lt;code&gt;send&lt;/code&gt; method to send the request to the server.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const pendingRequest: PendingRpcRequest&amp;lt;Slot&amp;gt; = rpc.getSlot();&#xA;&#xA;const slot: Slot = await pendingRequest.send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The arguments of the &lt;code&gt;getSlot&lt;/code&gt; method are reserved for the request payload, but the &lt;code&gt;send&lt;/code&gt; method is where additional arguments such as an &lt;code&gt;AbortSignal&lt;/code&gt; can be accepted in the context of the request.&lt;/p&gt; &#xA;&lt;p&gt;Aborting RPC requests can be useful for a variety of things such as setting a timeout on a request or cancelling a request when a user navigates away from a page.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpc } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const rpc = createSolanaRpc(&#39;http://127.0.0.1:8900&#39;);&#xA;&#xA;// Create a new AbortController.&#xA;const abortController = new AbortController();&#xA;&#xA;// Abort the request when the user navigates away from the current page.&#xA;function onUserNavigateAway() {&#xA;    abortController.abort();&#xA;}&#xA;&#xA;// The request will be aborted if and only if the user navigates away from the page.&#xA;const slot = await rpc.getSlot().send({ abortSignal: abortController.signal });&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Read more about &lt;code&gt;AbortController&lt;/code&gt; here:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/API/AbortController&#34;&gt;Mozilla Developer Docs: &lt;code&gt;AbortController&lt;/code&gt;&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal&#34;&gt;Mozilla Developer Docs: &lt;code&gt;AbortSignal&lt;/code&gt;&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://javascript.info/fetch-abort&#34;&gt;JavaScript.info: Fetch: Abort&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h2&gt;RPC Subscriptions&lt;/h2&gt; &#xA;&lt;p&gt;Subscriptions in the legacy library do not allow custom retry logic and do not allow you to recover from potentially missed messages. The new version does away with silent retries, surfaces transport errors to your application, and gives you the opportunity to recover from gap events.&lt;/p&gt; &#xA;&lt;p&gt;The main package responsible for managing communication with RPC subscriptions is &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;. However, similarly to &lt;code&gt;@solana/rpc&lt;/code&gt;, this package also makes use of more granular packages. These packages are:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;: Contains all logic related to subscribing to Solana RPC notifications.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-subscriptions-api&lt;/code&gt;: Describes all Solana RPC subscriptions using types.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-subscriptions-transport-websocket&lt;/code&gt;: Provides a concrete implementation of an RPC Subscriptions transport using WebSockets.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-subscriptions-spec&lt;/code&gt;: Defines the JSON RPC spec for subscribing to RPC notifications.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-spec-types&lt;/code&gt;: Shared JSON RPC specifications types and helpers that are used by both &lt;code&gt;@solana/rpc&lt;/code&gt; and &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/rpc-types&lt;/code&gt;: Shared Solana RPC types and helpers that are used by both &lt;code&gt;@solana/rpc&lt;/code&gt; and &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt;.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Since the main &lt;code&gt;@solana/web3.js&lt;/code&gt; library also re-exports the &lt;code&gt;@solana/rpc-subscriptions&lt;/code&gt; package we will import RPC Subscriptions types and functions directly from the main library going forward.&lt;/p&gt; &#xA;&lt;h3&gt;Getting Started with RPC Subscriptions&lt;/h3&gt; &#xA;&lt;p&gt;To get started with RPC Subscriptions, you may use the &lt;code&gt;createSolanaRpcSubscriptions&lt;/code&gt; function by providing the WebSocket URL of a Solana JSON RPC server. This will create a default client for interacting with Solana RPC Subscriptions.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpcSubscriptions } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create an RPC Subscriptions client.&#xA;const rpcSubscriptions = createSolanaRpcSubscriptions(&#39;ws://127.0.0.1:8900&#39;);&#xA;//    ^? RpcSubscriptions&amp;lt;SolanaRpcSubscriptionsApi&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Subscriptions as &lt;code&gt;AsyncIterators&lt;/code&gt;&lt;/h3&gt; &#xA;&lt;p&gt;The new subscriptions API vends subscription notifications as an &lt;code&gt;AsyncIterator&lt;/code&gt;. The &lt;code&gt;AsyncIterator&lt;/code&gt; conforms to the &lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#the_async_iterator_and_async_iterable_protocols&#34;&gt;async iterator protocol&lt;/a&gt;, which allows developers to consume messages using a &lt;code&gt;for await...of&lt;/code&gt; loop.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of working with a subscription in the new library:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { address, createSolanaRpcSubscriptions, createDefaultRpcSubscriptionsTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create the RPC Subscriptions client.&#xA;const rpcSubscriptions = createSolanaRpcSubscriptions(&#39;ws://127.0.0.1:8900&#39;);&#xA;&#xA;// Set up an abort controller.&#xA;const abortController = new AbortController();&#xA;&#xA;// Subscribe to account notifications.&#xA;const accountNotifications = await rpcSubscriptions&#xA;    .accountNotifications(address(&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;), { commitment: &#39;confirmed&#39; })&#xA;    .subscribe({ abortSignal: abortController.signal });&#xA;&#xA;try {&#xA;    // Consume messages.&#xA;    for await (const notification of accountNotifications) {&#xA;        console.log(&#39;New balance&#39;, notification.value.lamports);&#xA;    }&#xA;} catch (e) {&#xA;    // The subscription went down.&#xA;    // Retry it and then recover from potentially having missed&#xA;    // a balance update, here (eg. by making a `getBalance()` call).&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;You can read more about &lt;code&gt;AsyncIterator&lt;/code&gt; at the following links:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/AsyncIterator&#34;&gt;Mozilla Developer Docs: &lt;code&gt;AsyncIterator&lt;/code&gt;&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://www.nodejsdesignpatterns.com/blog/javascript-async-iterators/&#34;&gt;Luciano Mammino (Blog): JavaScript Async Iterators&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h3&gt;Aborting RPC Subscriptions&lt;/h3&gt; &#xA;&lt;p&gt;Similarly to RPC calls, applications can terminate active subscriptions using an &lt;code&gt;AbortController&lt;/code&gt; attribute on the &lt;code&gt;subscribe&lt;/code&gt; method. In fact, this parameter is &lt;em&gt;required&lt;/em&gt; for subscriptions to encourage you to clean up subscriptions that your application no longer needs.&lt;/p&gt; &#xA;&lt;p&gt;Let&#39;s take a look at some concrete examples that demonstrate how to abort subscriptions.&lt;/p&gt; &#xA;&lt;h4&gt;Subscription Timeout&lt;/h4&gt; &#xA;&lt;p&gt;Here&#39;s an example of an &lt;code&gt;AbortController&lt;/code&gt; used to abort a subscription after a 5-second timeout:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpcSubscriptions } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const rpcSubscriptions = createSolanaRpcSubscriptions(&#39;ws://127.0.0.1:8900&#39;);&#xA;&#xA;// Subscribe for slot notifications using an AbortSignal that times out after 5 seconds.&#xA;const slotNotifications = await rpcSubscriptions&#xA;    .slotNotifications()&#xA;    .subscribe({ abortSignal: AbortSignal.timeout(5000) });&#xA;&#xA;// Log slot notifications.&#xA;for await (const notification of slotNotifications) {&#xA;    console.log(&#39;Slot notification&#39;, notification);&#xA;}&#xA;&#xA;console.log(&#39;Done.&#39;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Read more about &lt;code&gt;AbortController&lt;/code&gt; at the following links:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/API/AbortController&#34;&gt;Mozilla Developer Docs: &lt;code&gt;AbortController&lt;/code&gt;&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal&#34;&gt;Mozilla Developer Docs: &lt;code&gt;AbortSignal&lt;/code&gt;&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://javascript.info/fetch-abort&#34;&gt;JavaScript.info: Fetch: Abort&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h4&gt;Cancelling Subscriptions&lt;/h4&gt; &#xA;&lt;p&gt;It is also possible to abort a subscription inside the &lt;code&gt;for await...of&lt;/code&gt; loop. This enables us to cancel a subscription based on some condition, such as a change in the state of an account. For instance, the following example cancels a subscription when the owner of an account changes:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;// Subscribe to account notifications.&#xA;const accountNotifications = await rpc&#xA;    .accountNotifications(address(&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;), { commitment: &#39;confirmed&#39; })&#xA;    .subscribe({ abortSignal });&#xA;&#xA;// Consume messages.&#xA;let previousOwner = null;&#xA;for await (const notification of accountNotifications) {&#xA;    const {&#xA;        value: { owner },&#xA;    } = notification;&#xA;    // Check the owner to see if it has changed&#xA;    if (previousOwner &amp;amp;&amp;amp; owner !== previousOwner) {&#xA;        // If so, abort the subscription&#xA;        abortController.abort();&#xA;    } else {&#xA;        console.log(notification);&#xA;    }&#xA;    previousOwner = owner;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Failed vs. Aborted Subscriptions&lt;/h3&gt; &#xA;&lt;p&gt;It is important to note that a subscription failure behaves differently from a subscription abort. A subscription failure occurs when the subscription goes down and will throw an error that can be intercepted in a &lt;code&gt;try/catch&lt;/code&gt;. However, an aborted subscription will not throw an error, but will instead exit the &lt;code&gt;for await...of&lt;/code&gt; loop.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;try {&#xA;    for await (const notification of notifications) {&#xA;        // Consume messages.&#xA;    }&#xA;    // [ABORTED] Reaching this line means the subscription was aborted — i.e. unsubscribed.&#xA;} catch (e) {&#xA;    // [FAILED] Reaching this line means the subscription went down.&#xA;    // Retry it, then recover from potential missed messages.&#xA;} finally {&#xA;    // [ABORTED or FAILED] Whether the subscription failed or was aborted, you can run cleanup code here.&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Message Gap Recovery&lt;/h3&gt; &#xA;&lt;p&gt;One of the most crucial aspects of any subscription API is managing potential missed messages. Missing messages, such as account state updates, could be catastrophic for an application. That’s why the new library provides native support for recovering missed messages using the &lt;code&gt;AsyncIterator&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;When a connection fails unexpectedly, any messages you miss while disconnected can result in your UI falling behind or becoming corrupt. Because subscription failure is now made explicit in the new API, you can implement ‘catch-up’ logic after re-establishing the subscription.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of such logic:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;try {&#xA;    for await (const notif of accountNotifications) {&#xA;        updateAccountBalance(notif.lamports);&#xA;    }&#xA;} catch (e) {&#xA;    // The subscription failed.&#xA;    // First, re-establish the subscription.&#xA;    await setupAccountBalanceSubscription(address);&#xA;    // Then make a one-shot request to &#39;catch up&#39; on any missed balance changes.&#xA;    const { value: lamports } = await rpc.getBalance(address).send();&#xA;    updateAccountBalance(lamports);&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Using Custom RPC Subscriptions Transports&lt;/h3&gt; &#xA;&lt;p&gt;The &lt;code&gt;createSolanaRpcSubscriptions&lt;/code&gt; function communicates with the RPC server using a default WebSocket transport that should satisfy most use cases. However, you may here as well provide your own transport or decorate existing ones to communicate with RPC servers in any way you see fit. In the example below, we explicitly create a WebSocket transport and use it to create a new RPC Subscriptions client via the &lt;code&gt;createSolanaRpcSubscriptionsFromTransport&lt;/code&gt; function.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createDefaultRpcSubscriptionsTransport, createSolanaRpcSubscriptionsFromTransport } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create a WebSocket transport or any custom transport of your choice.&#xA;const transport = createDefaultRpcSubscriptionsTransport({ url: &#39;ws://127.0.0.1:8900&#39; });&#xA;&#xA;// Create an RPC client using that transport.&#xA;const rpcSubscriptions = createSolanaRpcSubscriptionsFromTransport(transport);&#xA;//    ^? RpcSubscriptions&amp;lt;SolanaRpcSubscriptionsApi&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Augmenting/Constraining the RPC Subscriptions API&lt;/h3&gt; &#xA;&lt;p&gt;Using the &lt;code&gt;createSolanaRpcSubscriptions&lt;/code&gt; or &lt;code&gt;createSolanaRpcSubscriptionsFromTransport&lt;/code&gt; functions, we always get the same RPC Subscriptions API, including all Solana RPC stable subscriptions. However, since the RPC Subscriptions API is described using types only, it is possible to constrain the API to a specific set of subscriptions or even add your own custom subscriptions.&lt;/p&gt; &#xA;&lt;h4&gt;Constraining by Cluster&lt;/h4&gt; &#xA;&lt;p&gt;If you&#39;re using a specific cluster, you may wrap your RPC URL inside a helper function like &lt;code&gt;mainnet&lt;/code&gt; or &lt;code&gt;devnet&lt;/code&gt; to inject that information into the RPC type system.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { createSolanaRpcSubscriptions, mainnet, devnet } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const mainnetRpc = createSolanaRpcSubscriptions(mainnet(&#39;https://api.mainnet-beta.solana.com&#39;));&#xA;//    ^? RpcSubscriptionsMainnet&amp;lt;SolanaRpcSubscriptionsApi&amp;gt;&#xA;&#xA;const devnetRpc = createSolanaRpcSubscriptions(devnet(&#39;https://api.devnet.solana.com&#39;));&#xA;//    ^? RpcSubscriptionsDevnet&amp;lt;SolanaRpcSubscriptionsApi&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h4&gt;Including Unstable Subscriptions&lt;/h4&gt; &#xA;&lt;p&gt;If your app needs access to &lt;a href=&#34;https://docs.solana.com/api/websocket#blocksubscribe&#34;&gt;unstable RPC Subscriptions&lt;/a&gt; — e.g. &lt;code&gt;BlockNotificationsApi&lt;/code&gt; or &lt;code&gt;SlotsUpdatesNotificationsApi&lt;/code&gt; — and your RPC server supports them, you may use the &lt;code&gt;createSolanaRpcSubscriptions_UNSTABLE&lt;/code&gt; and &lt;code&gt;createSolanaRpcSubscriptionsFromTransport_UNSTABLE&lt;/code&gt; functions to create an RPC Subscriptions client that includes those subscriptions.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import {&#xA;    createSolanaRpcSubscriptions_UNSTABLE,&#xA;    createSolanaRpcSubscriptionsFromTransport_UNSTABLE,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Using the default WebSocket transport.&#xA;const rpcSubscriptions = createSolanaRpcSubscriptions_UNSTABLE(&#39;ws://127.0.0.1:8900&#39;);&#xA;//    ^? RpcSubscriptions&amp;lt;SolanaRpcSubscriptionsApi &amp;amp; SolanaRpcSubscriptionsApiUnstable&amp;gt;&#xA;&#xA;// Using a custom transport.&#xA;const transport = createDefaultRpcSubscriptionsTransport({ url: &#39;ws://127.0.0.1:8900&#39; });&#xA;const rpcSubscriptions = createSolanaRpcSubscriptionsFromTransport_UNSTABLE(transport);&#xA;//    ^? RpcSubscriptions&amp;lt;SolanaRpcSubscriptionsApi &amp;amp; SolanaRpcSubscriptionsApiUnstable&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h4&gt;Cherry-Picking API Methods&lt;/h4&gt; &#xA;&lt;p&gt;You may constrain the scope of the Subscription API even further so you are left only with the subscriptions you need. The simplest way to do this is to cast the created RPC client to a type that only includes the methods you need.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import {&#xA;    createSolanaRpcSubscriptions,&#xA;    type RpcSubscriptions,&#xA;    type AccountNotificationsApi,&#xA;    type SlotNotificationsApi,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;const rpc = createSolanaRpcSubscriptions(&#39;ws://127.0.0.1:8900&#39;) as RpcSubscriptions&amp;lt;&#xA;    AccountNotificationsApi &amp;amp; SlotNotificationsApi&#xA;&amp;gt;;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Alternatively, you may explicitly create the RPC Subscriptions API using the &lt;code&gt;createSolanaRpcSubscriptionsApi&lt;/code&gt; function. You will then need to create your own transport explicitly and bind the two together using the &lt;code&gt;createSubscriptionRpc&lt;/code&gt; function.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import {&#xA;    createDefaultRpcSubscriptionsTransport,&#xA;    createSubscriptionRpc,&#xA;    createSolanaRpcSubscriptionsApi,&#xA;    DEFAULT_RPC_CONFIG,&#xA;    type AccountNotificationsApi,&#xA;    type SlotNotificationsApi,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;const api = createSolanaRpcSubscriptionsApi&amp;lt;AccountNotificationsApi &amp;amp; SlotNotificationsApi&amp;gt;(DEFAULT_RPC_CONFIG);&#xA;const transport = createDefaultRpcSubscriptionsTransport({ url: &#39;ws://127.0.0.1:8900&#39; });&#xA;const rpcSubscriptions = createSubscriptionRpc({ api, transport });&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Note that the &lt;code&gt;createSolanaRpcSubscriptionsApi&lt;/code&gt; function is a wrapper on top of the &lt;code&gt;createRpcSubscriptionsApi&lt;/code&gt; function which adds some Solana-specific transformers such as setting a default commitment on all methods or throwing an error when an integer overflow is detected.&lt;/p&gt; &#xA;&lt;h2&gt;Keys&lt;/h2&gt; &#xA;&lt;p&gt;The new library takes a brand-new approach to Solana key pairs and addresses, which will feel quite different from the classes &lt;code&gt;PublicKey&lt;/code&gt; and &lt;code&gt;Keypair&lt;/code&gt; from version 1.x.&lt;/p&gt; &#xA;&lt;h3&gt;Web Crypto API&lt;/h3&gt; &#xA;&lt;p&gt;All key operations now use the native Ed25519 implementation in JavaScript’s Web Crypto API.&lt;/p&gt; &#xA;&lt;p&gt;The API itself is designed to be a more reliably secure way to manage highly sensitive secret key information, but &lt;strong&gt;developers should still use extreme caution when dealing with secret key bytes in their applications&lt;/strong&gt;.&lt;/p&gt; &#xA;&lt;p&gt;One thing to note is that many operations from Web Crypto – such as importing, generating, signing, and verifying are now &lt;strong&gt;asynchronous&lt;/strong&gt;.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of generating a &lt;code&gt;CryptoKeyPair&lt;/code&gt; using the Web Crypto API and signing a message:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { generateKeyPair, signBytes, verifySignature } from &#39;@solana/web3.js&#39;;&#xA;&#xA;const keyPair: CryptoKeyPair = await generateKeyPair();&#xA;&#xA;const message = new Uint8Array(8).fill(0);&#xA;&#xA;const signedMessage = await signBytes(keyPair.privateKey, message);&#xA;//    ^? Signature&#xA;&#xA;const verified = await verifySignature(keyPair.publicKey, signedMessage, message);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Web Crypto Polyfill&lt;/h3&gt; &#xA;&lt;p&gt;Wherever Ed25519 is not supported, we offer a polyfill for Web Crypto’s Ed25519 API.&lt;/p&gt; &#xA;&lt;p&gt;This polyfill can be found at &lt;code&gt;@solana/webcrypto-ed25519-polyfill&lt;/code&gt; and mimics the functionality of the Web Crypto API for Ed25519 key pairs using the same userspace implementation we used in web3.js 1.x. It does not polyfill other algorithms.&lt;/p&gt; &#xA;&lt;p&gt;Determine if your target runtime supports Ed25519, and install the polyfill if it does not:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { install } from &#39;@solana/webcrypto-ed25519-polyfill&#39;;&#xA;import { generateKeyPair, signBytes, verifySignature } from &#39;@solana/web3.js&#39;;&#xA;&#xA;install();&#xA;const keyPair: CryptoKeyPair = await generateKeyPair();&#xA;&#xA;/* Remaining logic */&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;You can see where Ed25519 is currently supported in &lt;a href=&#34;https://github.com/WICG/webcrypto-secure-curves/issues/20&#34;&gt;this GitHub issue&lt;/a&gt; on the Web Crypto repository. Consider sniffing the user-agent when deciding whether or not to deliver the polyfill to browsers.&lt;/p&gt; &#xA;&lt;p&gt;Operations on &lt;code&gt;CryptoKey&lt;/code&gt; objects using the Web Crypto API &lt;em&gt;or&lt;/em&gt; the polyfill are mostly handled by the &lt;code&gt;@solana/keys&lt;/code&gt; package.&lt;/p&gt; &#xA;&lt;h3&gt;String Addresses&lt;/h3&gt; &#xA;&lt;p&gt;All addresses are now JavaScript strings. They are represented by the opaque type &lt;code&gt;Address&lt;/code&gt;, which describes exactly what a Solana address actually is.&lt;/p&gt; &#xA;&lt;p&gt;Consequently, that means no more &lt;code&gt;PublicKey&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;Here’s what they look like in development:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { Address, address, getAddressFromPublicKey, generateKeyPair } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Coerce a string to an `Address`&#xA;const myOtherAddress = address(&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;);&#xA;&#xA;// Typecast it instead&#xA;const myAddress =&#xA;    &#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39; as Address&amp;lt;&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;&amp;gt;;&#xA;&#xA;// From CryptoKey&#xA;const keyPair = await generateKeyPair();&#xA;const myPublicKeyAsAddress = await getAddressFromPublicKey(keyPair.publicKey);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Some tooling for working with base58-encoded addresses can be found in the &lt;code&gt;@solana/addresses&lt;/code&gt; package.&lt;/p&gt; &#xA;&lt;h2&gt;Transactions&lt;/h2&gt; &#xA;&lt;h3&gt;Creating Transaction Messages&lt;/h3&gt; &#xA;&lt;p&gt;Like many other familiar aspects of the 1.0 library, transactions have received a makeover.&lt;/p&gt; &#xA;&lt;p&gt;For starters, all transaction messages are now version-aware, so there’s no longer a need to juggle two different types (eg. &lt;code&gt;Transaction&lt;/code&gt; vs. &lt;code&gt;VersionedTransaction&lt;/code&gt;).&lt;/p&gt; &#xA;&lt;p&gt;Address lookups are now completely described inside transaction message instructions, so you don’t have to materialize &lt;code&gt;addressTableLookups&lt;/code&gt; anymore.&lt;/p&gt; &#xA;&lt;p&gt;Here’s a simple example of creating a transaction message – notice how its type is refined at each step of the process:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import {&#xA;    address,&#xA;    createTransactionMessage,&#xA;    setTransactionMessageFeePayer,&#xA;    setTransactionMessageLifetimeUsingBlockhash,&#xA;    Blockhash,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;const recentBlockhash = {&#xA;    blockhash: &#39;4uhcVJyU9pJkvQyS88uRDiswHXSCkY3zQawwpjk2NsNY&#39; as Blockhash,&#xA;    lastValidBlockHeight: 196055492n,&#xA;};&#xA;const feePayer = address(&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;);&#xA;&#xA;// Create a new transaction message&#xA;const transactionMessage = createTransactionMessage({ version: 0 });&#xA;//    ^? V0TransactionMessage&#xA;&#xA;// Set the fee payer&#xA;const transactionMessageWithFeePayer = setTransactionMessageFeePayer(feePayer, transactionMessage);&#xA;//    ^? V0TransactionMessage &amp;amp; ITransactionMessageWithFeePayer&#xA;&#xA;const transactionMessageWithFeePayerAndLifetime = setTransactionMessageLifetimeUsingBlockhash(&#xA;    // ^? V0TransactionMessage &amp;amp; ITransactionMessageWithFeePayer &amp;amp; TransactionMessageWithBlockhashLifetime&#xA;    recentBlockhash,&#xA;    transactionMessageWithFeePayer,&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;As you can see, each time a transaction message is modified, the type reflects its new shape. If you add a fee payer, you’ll get a type representing a transaction message with a fee payer, and so on.&lt;/p&gt; &#xA;&lt;p&gt;Transaction message objects are also &lt;strong&gt;frozen by these functions&lt;/strong&gt; to prevent them from being mutated in place.&lt;/p&gt; &#xA;&lt;h3&gt;Signing Transaction Messages&lt;/h3&gt; &#xA;&lt;p&gt;The &lt;code&gt;signTransaction(..)&lt;/code&gt; function will raise a type error if your transaction message is not already equipped with a fee payer and a lifetime. This helps you catch errors at author-time instead of runtime.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const feePayer = address(&#39;AxZfZWeqztBCL37Mkjkd4b8Hf6J13WCcfozrBY6vZzv3&#39;);&#xA;const signer = await generateKeyPair();&#xA;&#xA;const transactionMessage = createTransactionMessage({ version: &#39;legacy&#39; });&#xA;const transactionMessageWithFeePayer = setTransactionMessageFeePayer(feePayer, transactionMessage);&#xA;&#xA;// Attempting to sign the transaction message without a lifetime will throw a type error&#xA;const signedTransaction = await signTransaction([signer], transactionMessageWithFeePayer);&#xA;// =&amp;gt; &#34;Property &#39;lifetimeConstraint&#39; is missing in type&#34;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Calibrating a Transaction Message&#39;s Compute Unit Budget&lt;/h3&gt; &#xA;&lt;p&gt;Correctly budgeting a compute unit limit for your transaction message can increase the probability that your transaction will be accepted for processing. If you don&#39;t declare a compute unit limit on your transaction, validators will assume an upper limit of 200K compute units (CU) per instruction.&lt;/p&gt; &#xA;&lt;p&gt;Since validators have an incentive to pack as many transactions into each block as possible, they may choose to include transactions that they know will fit into the remaining compute budget for the current block over transactions that might not. For this reason, you should set a compute unit limit on each of your transaction messages, whenever possible.&lt;/p&gt; &#xA;&lt;p&gt;Use this utility to estimate the actual compute unit cost of a given transaction message.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { getSetComputeUnitLimitInstruction } from &#39;@solana-program/compute-budget&#39;;&#xA;import { createSolanaRpc, getComputeUnitEstimateForTransactionMessageFactory, pipe } from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Create an estimator function.&#xA;const rpc = createSolanaRpc(&#39;http://127.0.0.1:8899&#39;);&#xA;const getComputeUnitEstimateForTransactionMessage = getComputeUnitEstimateForTransactionMessageFactory({&#xA;    rpc,&#xA;});&#xA;&#xA;// Create your transaction message.&#xA;const transactionMessage = pipe(&#xA;    createTransactionMessage({ version: &#39;legacy&#39; }),&#xA;    /* ... */&#xA;);&#xA;&#xA;// Request an estimate of the actual compute units this message will consume.&#xA;const computeUnitsEstimate = await getComputeUnitEstimateForTransactionMessage(transactionMessage);&#xA;&#xA;// Set the transaction message&#39;s compute unit budget.&#xA;const transactionMessageWithComputeUnitLimit = prependTransactionMessageInstruction(&#xA;    getSetComputeUnitLimitInstruction({ units: computeUnitsEstimate }),&#xA;    transactionMessage,&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;blockquote&gt; &#xA; &lt;p&gt;[!WARNING] The compute unit estimate is just that – an estimate. The compute unit consumption of the actual transaction might be higher or lower than what was observed in simulation. Unless you are confident that your particular transaction message will consume the same or fewer compute units as was estimated, you might like to augment the estimate by either a fixed number of CUs or a multiplier.&lt;/p&gt; &#xA;&lt;/blockquote&gt; &#xA;&lt;blockquote&gt; &#xA; &lt;p&gt;[!NOTE] If you are preparing an &lt;em&gt;unsigned&lt;/em&gt; transaction, destined to be signed and submitted to the network by a wallet, you might like to leave it up to the wallet to determine the compute unit limit. Consider that the wallet might have a more global view of how many compute units certain types of transactions consume, and might be able to make better estimates of an appropriate compute unit budget.&lt;/p&gt; &#xA;&lt;/blockquote&gt; &#xA;&lt;h3&gt;Helpers For Building Transaction Messages&lt;/h3&gt; &#xA;&lt;p&gt;Building transaction messages in this manner might feel different from what you’re used to. Also, we certainly wouldn’t want you to have to bind transformed transaction messages to a new variable at each step, so we have released a functional programming library dubbed &lt;code&gt;@solana/functional&lt;/code&gt; that lets you build transaction messages in &lt;strong&gt;pipelines&lt;/strong&gt;. Here’s how it can be used:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { pipe } from &#39;@solana/functional&#39;;&#xA;import {&#xA;    address,&#xA;    createTransactionMessage,&#xA;    setTransactionMessageFeePayer,&#xA;    setTransactionMessageLifetimeUsingBlockhash,&#xA;    Blockhash,&#xA;} from &#39;@solana/web3.js&#39;;&#xA;&#xA;// Use `pipe(..)` to create a pipeline of transaction message transformation operations&#xA;const transactionMessage = pipe(&#xA;    createTransactionMessage({ version: 0 }),&#xA;    tx =&amp;gt; setTransactionMessageFeePayer(feePayer, tx),&#xA;    tx =&amp;gt; setTransactionMessageLifetimeUsingBlockhash(recentBlockhash, tx),&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Note that &lt;code&gt;pipe(..)&lt;/code&gt; is general-purpose, so it can be used to pipeline any functional transforms.&lt;/p&gt; &#xA;&lt;h2&gt;Codecs&lt;/h2&gt; &#xA;&lt;p&gt;We have taken steps to make it easier to write data (de)serializers, especially as they pertain to Rust datatypes and byte buffers.&lt;/p&gt; &#xA;&lt;p&gt;Solana’s codecs libraries are broken up into modular components so you only need to import the ones you need. They are:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/codecs-core&lt;/code&gt;: The core codecs library for working with codecs serializers and creating custom ones&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/codecs-numbers&lt;/code&gt;: Used for serialization of numbers (little-endian and big-endian bytes, etc.)&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/codecs-strings&lt;/code&gt;: Used for serialization of strings&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/codecs-data-structures&lt;/code&gt;: Codecs and serializers for structs&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;@solana/options&lt;/code&gt;: Designed to build codecs and serializers for types that mimic Rust’s enums, which can include embedded data within their variants such as values, tuples, and structs&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;These packages are included in the main &lt;code&gt;@solana/web3.js&lt;/code&gt; library but you may also import them from &lt;code&gt;@solana/codecs&lt;/code&gt; if you only need the codecs.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of encoding and decoding a custom struct with some strings and numbers:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { addCodecSizePrefix } from &#39;@solana/codecs-core&#39;;&#xA;import { getStructCodec } from &#39;@solana/codecs-data-structures&#39;;&#xA;import { getU32Codec, getU64Codec, getU8Codec } from &#39;@solana/codecs-numbers&#39;;&#xA;import { getUtf8Codec } from &#39;@solana/codecs-strings&#39;;&#xA;&#xA;// Equivalent in Rust:&#xA;// struct {&#xA;//     amount: u64,&#xA;//     decimals: u8,&#xA;//     name: String,&#xA;// }&#xA;const structCodec = getStructCodec([&#xA;    [&#39;amount&#39;, getU64Codec()],&#xA;    [&#39;decimals&#39;, getU8Codec()],&#xA;    [&#39;name&#39;, addCodecSizePrefix(getUtf8Codec(), getU32Codec())],&#xA;]);&#xA;&#xA;const myToken = {&#xA;    amount: 1000000000000000n, // `bigint` or `number` is supported&#xA;    decimals: 2,&#xA;    name: &#39;My Token&#39;,&#xA;};&#xA;&#xA;const myEncodedToken: Uint8Array = structCodec.encode(myToken);&#xA;const myDecodedToken = structCodec.decode(myEncodedToken);&#xA;&#xA;myDecodedToken satisfies {&#xA;    amount: bigint;&#xA;    decimals: number;&#xA;    name: string;&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;You may only need to encode or decode data, but not both. Importing one or the other allows your optimizing compiler to tree-shake the other implementation away:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { Codec, combineCodec, Decoder, Encoder, addDecoderSizePrefix, addEncoderSizePrefix } from &#39;@solana/codecs-core&#39;;&#xA;import { getStructDecoder, getStructEncoder } from &#39;@solana/codecs-data-structures&#39;;&#xA;import {&#xA;    getU8Decoder,&#xA;    getU8Encoder,&#xA;    getU32Decoder,&#xA;    getU32Encoder,&#xA;    getU64Decoder,&#xA;    getU64Encoder,&#xA;} from &#39;@solana/codecs-numbers&#39;;&#xA;import { getUtf8Decoder, getUtf8Encoder } from &#39;@solana/codecs-strings&#39;;&#xA;&#xA;export type MyToken = {&#xA;    amount: bigint;&#xA;    decimals: number;&#xA;    name: string;&#xA;};&#xA;&#xA;export type MyTokenArgs = {&#xA;    amount: number | bigint;&#xA;    decimals: number;&#xA;    name: string;&#xA;};&#xA;&#xA;export const getMyTokenEncoder = (): Encoder&amp;lt;MyTokenArgs&amp;gt; =&amp;gt;&#xA;    getStructEncoder([&#xA;        [&#39;amount&#39;, getU64Encoder()],&#xA;        [&#39;decimals&#39;, getU8Encoder()],&#xA;        [&#39;name&#39;, addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],&#xA;    ]);&#xA;&#xA;export const getMyTokenDecoder = (): Decoder&amp;lt;MyToken&amp;gt; =&amp;gt;&#xA;    getStructDecoder([&#xA;        [&#39;amount&#39;, getU64Decoder()],&#xA;        [&#39;decimals&#39;, getU8Decoder()],&#xA;        [&#39;name&#39;, addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],&#xA;    ]);&#xA;&#xA;export const getMyTokenCodec = (): Codec&amp;lt;MyTokenArgs, MyToken&amp;gt; =&amp;gt;&#xA;    combineCodec(getMyTokenEncoder(), getMyTokenDecoder());&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;You can read me about codecs in &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/raw/master/packages/codecs/README.md&#34;&gt;the official Codec documentation&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Type-Safety&lt;/h2&gt; &#xA;&lt;p&gt;The new library makes use of some advanced TypeScript features, including generic types, conditional types, &lt;code&gt;Parameters&amp;lt;..&amp;gt;&lt;/code&gt;, &lt;code&gt;ReturnType&amp;lt;..&amp;gt;&lt;/code&gt; and more.&lt;/p&gt; &#xA;&lt;p&gt;We’ve described the RPC API in detail so that TypeScript can determine the &lt;em&gt;exact&lt;/em&gt; type of the result you will receive from the server given a particular input. Change the type of the input, and you will see the return type reflect that change.&lt;/p&gt; &#xA;&lt;h3&gt;RPC Types&lt;/h3&gt; &#xA;&lt;p&gt;The RPC methods – both HTTP and subscriptions – are built with multiple overloads and conditional types. The expected HTTP response payload or subscription message format will be reflected in the return type of the function you’re working with when you provide the inputs in your code.&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of this in action:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;// Provide one set of parameters, get a certain type&#xA;// These parameters resolve to return type:&#xA;// {&#xA;//     blockhash: Blockhash;&#xA;//     blockHeight: bigint;&#xA;//     blockTime: UnixTimestamp;&#xA;//     parentSlot: bigint;&#xA;//     previousBlockhash: Blockhash;&#xA;// }&#xA;const blockResponse = await rpc&#xA;    .getBlock(0n, {&#xA;        rewards: false,&#xA;        transactionDetails: &#39;none&#39;,&#xA;    })&#xA;    .send();&#xA;&#xA;// Switch `rewards` to `true`, get `rewards` in the return type&#xA;// {&#xA;//     /* ... Previous response */&#xA;//     rewards: Reward[];&#xA;// }&#xA;const blockWithRewardsResponse = await rpc&#xA;    .getBlock(0n, {&#xA;        rewards: true,&#xA;        transactionDetails: &#39;none&#39;,&#xA;    })&#xA;    .send();&#xA;&#xA;// Switch `transactionDetails` to `full`, get `transactions` in the return type&#xA;// {&#xA;//     /* ... Previous response */&#xA;//     transactions: TransactionResponse[];&#xA;// }&#xA;const blockWithRewardsAndTransactionsResponse = await rpc&#xA;    .getBlock(0n, {&#xA;        rewards: true,&#xA;        transactionDetails: &#39;full&#39;,&#xA;    })&#xA;    .send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Catching Compile-Time Bugs with TypeScript&lt;/h3&gt; &#xA;&lt;p&gt;As previously mentioned, the type coverage in version 2.0 allows developers to catch common bugs at compile time, rather than runtime.&lt;/p&gt; &#xA;&lt;p&gt;In the example below, a transaction message is created and then attempted to be signed without setting the fee payer. This would result in a runtime error from the RPC, but instead you will see a type error from TypeScript as you type:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const transactionMessage = pipe(createTransactionMessage({ version: 0 }), tx =&amp;gt;&#xA;    setTransactionMessageLifetimeUsingBlockhash(recentBlockhash, tx),&#xA;);&#xA;const signedTransaction = await signTransaction([keyPair], transactionMessage); // ERROR: Property &#39;feePayer&#39; is missing in type&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Consider another example where a developer is attempting to send a transaction that has not been fully signed. Again, the TypeScript compiler will throw a type error:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const transactionMessage = pipe(&#xA;    createTransactionMessage({ version: 0 }),&#xA;    tx =&amp;gt; setTransactionMessageFeePayer(feePayerAddress, tx),&#xA;    tx =&amp;gt; setTransactionMessageLifetimeUsingBlockhash(recentBlockhash, tx),&#xA;);&#xA;&#xA;const signedTransaction = await signTransaction([], transactionMessage);&#xA;&#xA;// Asserts the transaction is a `FullySignedTransaction`&#xA;// Throws an error if any signatures are missing!&#xA;assertTransactionIsFullySigned(signedTransaction);&#xA;&#xA;await sendAndConfirmTransaction(signedTransaction);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Are you building a nonce transaction and forgot to make &lt;code&gt;AdvanceNonce&lt;/code&gt; the first instruction? That’s a type error:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const feePayer = await generateKeyPair();&#xA;const feePayerAddress = await getAddressFromPublicKey(feePayer.publicKey);&#xA;&#xA;const notNonceTransactionMessage = pipe(createTransactionMessage({ version: 0 }), tx =&amp;gt;&#xA;    setTransactionMessageFeePayer(feePayerAddress, tx),&#xA;);&#xA;&#xA;notNonceTransactionMessage satisfies TransactionMessageWithDurableNonceLifetime;&#xA;// =&amp;gt; Property &#39;lifetimeConstraint&#39; is missing in type&#xA;&#xA;const nonceConfig = {&#xA;    nonce: &#39;nonce&#39; as Nonce,&#xA;    nonceAccountAddress: address(&#39;5tLU66bxQ35so2bReGcyf3GfMMAAauZdNA1N4uRnKQu4&#39;),&#xA;    nonceAuthorityAddress: address(&#39;GDhj8paPg8woUzp9n8fj7eAMocN5P7Ej3A7T9F5gotTX&#39;),&#xA;};&#xA;&#xA;const stillNotNonceTransactionMessage = {&#xA;    lifetimeConstraint: nonceConfig,&#xA;    ...notNonceTransactionMessage,&#xA;};&#xA;&#xA;stillNotNonceTransactionMessage satisfies TransactionMessageWithDurableNonceLifetime;&#xA;// =&amp;gt; &#39;readonly IInstruction&amp;lt;string&amp;gt;[]&#39; is not assignable to type &#39;readonly [AdvanceNonceAccountInstruction&amp;lt;string, string&amp;gt;, ...IInstruction&amp;lt;string&amp;gt;[]]&#39;&#xA;&#xA;const validNonceTransactionMessage = pipe(&#xA;    createTransactionMessage({ version: 0 }),&#xA;    tx =&amp;gt; setTransactionMessageFeePayer(feePayerAddress, tx),&#xA;    tx =&amp;gt; setTransactionMessageLifetimeUsingDurableNonce(nonceConfig, tx), // Adds the instruction!&#xA;);&#xA;&#xA;validNonceTransactionMessage satisfies TransactionMessageWithDurableNonceLifetime; // OK&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The library’s type-checking can even catch you using lamports instead of SOL for a value:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const airdropAmount = 1n; // SOL&#xA;const signature = rpc.requestAirdrop(myAddress, airdropAmount).send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;It will force you to cast the numerical value for your airdrop (or transfer, etc.) amount using &lt;code&gt;lamports()&lt;/code&gt;, which should be a good reminder!&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const airdropAmount = lamports(1000000000n);&#xA;const signature = rpc.requestAirdrop(myAddress, airdropAmount).send();&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Compatibility Layer&lt;/h2&gt; &#xA;&lt;p&gt;You will have noticed by now that web3.js is a complete and total breaking change from the 1.x line. We want to provide you with a strategy for interacting with 1.x APIs while building your application using 2.0. You need a tool for commuting between 1.x and 2.0 data types.&lt;/p&gt; &#xA;&lt;p&gt;The &lt;code&gt;@solana/compat&lt;/code&gt; library allows for interoperability between functions and class objects from the legacy library - such as &lt;code&gt;VersionedTransaction&lt;/code&gt;, &lt;code&gt;PublicKey&lt;/code&gt;, and &lt;code&gt;Keypair&lt;/code&gt; - and functions and types of the new library - such as &lt;code&gt;Address&lt;/code&gt;, &lt;code&gt;Transaction&lt;/code&gt;, and &lt;code&gt;CryptoKeyPair&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;Here’s how you can use &lt;code&gt;@solana/compat&lt;/code&gt; to convert from a legacy &lt;code&gt;PublicKey&lt;/code&gt; to an &lt;code&gt;Address&lt;/code&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { fromLegacyPublicKey } from &#39;@solana/compat&#39;;&#xA;&#xA;const publicKey = new PublicKey(&#39;B3piXWBQLLRuk56XG5VihxR4oe2PSsDM8nTF6s1DeVF5&#39;);&#xA;const address: Address = fromLegacyPublicKey(publicKey);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Here’s how to convert from a legacy &lt;code&gt;Keypair&lt;/code&gt; to a &lt;code&gt;CryptoKeyPair&lt;/code&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { fromLegacyKeypair } from &#39;@solana/compat&#39;;&#xA;&#xA;const keypairLegacy = Keypair.generate();&#xA;const cryptoKeyPair: CryptoKeyPair = fromLegacyKeypair(keypair);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Here’s how to convert legacy transaction objects to the new library’s transaction types:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;// Note that you can only convert `VersionedTransaction` objects&#xA;const modernTransaction = fromVersionedTransaction(classicTransaction);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;To see more conversions supported by &lt;code&gt;@solana/compat&lt;/code&gt;, you can check out the package’s &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/raw/master/packages/compat/README.md&#34;&gt;README on GitHub&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Program Clients&lt;/h2&gt; &#xA;&lt;p&gt;Writing JavaScript clients for on-chain programs has been done manually up until now. Without an IDL for some of the native programs, this process has been necessarily manual and has resulted in clients that lag behind the actual capabilities of the programs themselves.&lt;/p&gt; &#xA;&lt;p&gt;We think that program clients should be &lt;em&gt;generated&lt;/em&gt; rather than written. Developers should be able to write Rust programs, compile the program code, and generate all of the JavaScript client-side code to interact with the program.&lt;/p&gt; &#xA;&lt;p&gt;We use &lt;a href=&#34;https://github.com/metaplex-foundation/kinobi&#34;&gt;Kinobi&lt;/a&gt; to represent Solana programs and generate clients for them. This includes a JavaScript client compatible with this library. For instance, here is how you’d construct a transaction message composed of instructions from three different core programs.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;import { appendTransactionMessageInstructions, createTransactionMessage, pipe } from &#39;@solana/web3.js&#39;;&#xA;import { getAddMemoInstruction } from &#39;@solana-program/memo&#39;;&#xA;import { getSetComputeUnitLimitInstruction } from &#39;@solana-program/compute-budget&#39;;&#xA;import { getTransferSolInstruction } from &#39;@solana-program/system&#39;;&#xA;&#xA;const instructions = [&#xA;    getSetComputeUnitLimitInstruction({ units: 600_000 }),&#xA;    getTransferSolInstruction({ source, destination, amount: 1_000_000_000 }),&#xA;    getAddMemoInstruction({ memo: &#34;I&#39;m transferring some SOL!&#34; }),&#xA;];&#xA;&#xA;// Creates a V0 transaction message with 3 instructions inside.&#xA;const transactionMessage = pipe(createTransactionMessage({ version: 0 }), tx =&amp;gt;&#xA;    appendTransactionMessageInstructions(instructions, tx),&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;As you can see, each program now generates its own library allowing you to cherry-pick your dependencies.&lt;/p&gt; &#xA;&lt;p&gt;Note that asynchronous versions may be available for some instructions which allows them to resolve more inputs on your behalf — such as PDA derivation. For instance, the &lt;code&gt;CreateLookupTable&lt;/code&gt; instruction offers an asynchronous builder that derives the &lt;code&gt;address&lt;/code&gt; account and the &lt;code&gt;bump&lt;/code&gt; argument for us.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const rpc = createSolanaRpc(&#39;http://127.0.0.1:8899&#39;);&#xA;const [authority, recentSlot] = await Promise.all([&#xA;    generateKeyPairSigner(),&#xA;    rpc.getSlot({ commitment: &#39;finalized&#39; }).send(),&#xA;]);&#xA;&#xA;const instruction = await getCreateLookupTableInstructionAsync({&#xA;    authority,&#xA;    recentSlot,&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Alternatively, you may use the synchronous builder if you already have all the required inputs at hand.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const [address, bump] = await findAddressLookupTablePda({&#xA;    authority: authority.address,&#xA;    recentSlot,&#xA;});&#xA;&#xA;const instruction = getCreateLookupTableInstruction({&#xA;    address,&#xA;    authority,&#xA;    bump,&#xA;    recentSlot,&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;On top of instruction builders, these clients offer a variety of utilities such as:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;Instruction codecs — e.g. &lt;code&gt;getTransferSolInstructionDataCodec&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Account types — e.g. &lt;code&gt;AddressLookupTable&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Account codecs — e.g. &lt;code&gt;getAddressLookupTableAccountDataCodec&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Account helpers — e.g. &lt;code&gt;fetchAddressLookupTable&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;PDA helpers — e.g. &lt;code&gt;findAddressLookupTablePda&lt;/code&gt;, &lt;code&gt;fetchAddressLookupTableFromSeeds&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Defined types and their codecs — e.g. &lt;code&gt;NonceState&lt;/code&gt;, &lt;code&gt;getNonceStateCodec&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;Program helpers — e.g. &lt;code&gt;SYSTEM_PROGRAM_ADDRESS&lt;/code&gt;, &lt;code&gt;SystemAccount&lt;/code&gt; enum, &lt;code&gt;SystemAccount&lt;/code&gt; enum, &lt;code&gt;identifySystemInstruction&lt;/code&gt;.&lt;/li&gt; &#xA; &lt;li&gt;And much more!&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Here’s another example that fetches an &lt;code&gt;AddressLookupTable&lt;/code&gt; PDA from its seeds.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const account = await fetchAddressLookupTableFromSeeds(rpc, {&#xA;    authority: authority.address,&#xA;    recentSlot,&#xA;});&#xA;&#xA;account.address; // Address&#xA;account.lamports; // LamportsUnsafeBeyond2Pow53Minus1&#xA;account.data.addresses; // Address[]&#xA;account.data.authority; // Some&amp;lt;Address&amp;gt;&#xA;account.data.deactivationSlot; // Slot&#xA;account.data.lastExtendedSlot; // Slot&#xA;account.data.lastExtendedSlotStartIndex; // number&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;How Does This Work?&lt;/h3&gt; &#xA;&lt;p&gt;All of this code is 100% auto-generated by Kinobi from a tree of standardized nodes that represent our programs. It contains obvious nodes such as &lt;code&gt;AccountNode&lt;/code&gt; but also more specified nodes such as &lt;code&gt;ConditionalValueNode&lt;/code&gt; that allows us to resolve account or argument default values conditionally.&lt;/p&gt; &#xA;&lt;p&gt;Kinobi allows us to hydrate our tree of nodes from IDLs which are typically generated by program frameworks such as &lt;a href=&#34;https://github.com/coral-xyz/anchor&#34;&gt;Anchor&lt;/a&gt; or &lt;a href=&#34;https://github.com/metaplex-foundation/shank&#34;&gt;Shank&lt;/a&gt;. Additionally, visitors can be used on our nodes to expand the knowledge of our programs since the IDL itself doesn’t yet contain that level of information. Finally, special visitors called ‘renderers’ visit our tree to generate clients such as this JavaScript client.&lt;/p&gt; &#xA;&lt;p&gt;Currently, there is one other renderer that generates Rust clients but this is only the beginning. In the future, you can expect renderers for auto-generated Python clients, documentation, CLIs, etc.&lt;/p&gt; &#xA;&lt;h2&gt;Create Solana Program&lt;/h2&gt; &#xA;&lt;p&gt;We believe the whole ecosystem could benefit from generated program clients. That’s why we introduced a new NPM binary that allows you to create your Solana program — and generate clients for it — in no time. Simply run the following and follow the prompts to get started.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-sh&#34;&gt;pnpm create solana-program&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;This &lt;a href=&#34;https://github.com/solana-program/create-solana-program&#34;&gt;&lt;code&gt;create-solana-program&lt;/code&gt;&lt;/a&gt; installer will create a new repository including:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;An example program using the framework of your choice (Anchor coming soon).&lt;/li&gt; &#xA; &lt;li&gt;Generated clients for any of the selected clients.&lt;/li&gt; &#xA; &lt;li&gt;A set of scripts that allows you to: &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;Start a local validator including all programs and accounts you depend on.&lt;/li&gt; &#xA;   &lt;li&gt;Build, lint and test your programs.&lt;/li&gt; &#xA;   &lt;li&gt;Generate IDLs from your programs.&lt;/li&gt; &#xA;   &lt;li&gt;Generate clients from the generated IDLs.&lt;/li&gt; &#xA;   &lt;li&gt;Build and test each of your clients.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;GitHub Actions pipelines to test your program, test your clients, and even manually publish new packages or crates for your clients. (Coming soon).&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;When selecting the JavaScript client, you will get a fully generated library compatible with the new web3.js much like the &lt;code&gt;@solana-program&lt;/code&gt; packages showcased above.&lt;/p&gt; &#xA;&lt;h2&gt;GraphQL&lt;/h2&gt; &#xA;&lt;p&gt;Though not directly related to web3.js, we wanted to hijack your attention to show you something else that we’re working on, of particular interest to frontend developers. It’s a new API for interacting with the RPC: a GraphQL API.&lt;/p&gt; &#xA;&lt;p&gt;The &lt;code&gt;@solana/rpc-graphql&lt;/code&gt; package can be used to make GraphQL queries to Solana RPC endpoints, using the same transports described above (including any customizations).&lt;/p&gt; &#xA;&lt;p&gt;Here’s an example of retrieving account data with GraphQL:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const source = `&#xA;    query myQuery($address: String!) {&#xA;        account(address: $address) {&#xA;            dataBase58: data(encoding: BASE_58)&#xA;            dataBase64: data(encoding: BASE_64)&#xA;            lamports&#xA;        }&#xA;    }&#xA;`;&#xA;&#xA;const variableValues = {&#xA;    address: &#39;AyGCwnwxQMCqaU4ixReHt8h5W4dwmxU7eM3BEQBdWVca&#39;,&#xA;};&#xA;&#xA;const result = await rpcGraphQL.query(source, variableValues);&#xA;&#xA;expect(result).toMatchObject({&#xA;    data: {&#xA;        account: {&#xA;            dataBase58: &#39;2Uw1bpnsXxu3e&#39;,&#xA;            dataBase64: &#39;dGVzdCBkYXRh&#39;,&#xA;            lamports: 10290815n,&#xA;        },&#xA;    },&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Using GraphQL allows developers to only specify which fields they &lt;em&gt;actually&lt;/em&gt; need, and do away with the rest of the response.&lt;/p&gt; &#xA;&lt;p&gt;However, GraphQL is also extremely powerful for &lt;strong&gt;nesting queries&lt;/strong&gt;, which can be particularly useful if you want to, say, get the &lt;strong&gt;sum&lt;/strong&gt; of every lamports balance of every &lt;strong&gt;owner of the owner&lt;/strong&gt; of each token account, while discarding any mint accounts.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const source = `&#xA;    query getLamportsOfOwnersOfOwnersOfTokenAccounts {&#xA;        programAccounts(programAddress: &#34;TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA&#34;) {&#xA;            ... on TokenAccount {&#xA;                owner {&#xA;                    ownerProgram {&#xA;                        lamports&#xA;                    }&#xA;                }&#xA;            }&#xA;        }&#xA;    }&#xA;`;&#xA;&#xA;const result = await rpcGraphQL.query(source);&#xA;&#xA;const sumOfAllLamportsOfOwnersOfOwnersOfTokenAccounts = result&#xA;    .map(o =&amp;gt; o.account.owner.ownerProgram.lamports)&#xA;    .reduce((acc, lamports) =&amp;gt; acc + lamports, 0);&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The new GraphQL package supports this same style of nested querying on transactions and blocks.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ts&#34;&gt;const source = `&#xA;    query myQuery($signature: String!, $commitment: Commitment) {&#xA;        transaction(signature: $signature, commitment: $commitment) {&#xA;            message {&#xA;                instructions {&#xA;                    ... on CreateAccountInstruction {&#xA;                        lamports&#xA;                        programId&#xA;                        space&#xA;                    }&#xA;                }&#xA;            }&#xA;        }&#xA;    }&#xA;`;&#xA;&#xA;const variableValues = {&#xA;    signature: &#39;63zkpxATgAwXRGFQZPDESTw2m4uZQ99sX338ibgKtTcgG6v34E3MSS3zckCwJHrimS71cvei6h1Bn1K1De53BNWC&#39;,&#xA;    commitment: &#39;confirmed&#39;,&#xA;};&#xA;&#xA;const result = await rpcGraphQL.query(source, variableValues);&#xA;&#xA;expect(result).toMatchObject({&#xA;    data: {&#xA;        transaction: {&#xA;            message: {&#xA;                instructions: expect.arrayContaining([&#xA;                    {&#xA;                        lamports: expect.any(BigInt),&#xA;                        programId: &#39;11111111111111111111111111111111&#39;,&#xA;                        space: expect.any(BigInt),&#xA;                    },&#xA;                ]),&#xA;            },&#xA;        },&#xA;    },&#xA;});&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;See more in the package’s &lt;a href=&#34;https://github.com/solana-labs/solana-web3.js/tree/master/packages/rpc-graphql&#34;&gt;README on GitHub&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Development&lt;/h2&gt; &#xA;&lt;p&gt;You can see all development of this library and associated GraphQL tooling in the web3.js repository on GitHub.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/solana-labs/solana-web3.js&#34;&gt;https://github.com/solana-labs/solana-web3.js&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;You can follow along with program client generator development in the &lt;code&gt;@solana-program&lt;/code&gt; org and the &lt;code&gt;@kinobi-so/kinobi&lt;/code&gt; repository.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/solana-program/&#34;&gt;https://github.com/solana-program/&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://github.com/kinobi-so/kinobi&#34;&gt;https://github.com/kinobi-so/kinobi&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;Solana Labs develops these tools in public, as open source. We encourage any and all developers who would like to work on these tools to contribute to the codebase.&lt;/p&gt; &#xA;&lt;h2&gt;Thank you&lt;/h2&gt; &#xA;&lt;p&gt;We’re grateful that you have read this far. If you are interested in migrating an existing application to the new web3.js to take advantage of some of the benefits we’ve demonstrated, we want to give you some direct support. Reach out to &lt;a href=&#34;https://t.me/steveluscher/&#34;&gt;@steveluscher&lt;/a&gt; on Telegram to start a conversation.&lt;/p&gt;</summary>
  </entry>
  <entry>
    <title>ItzCrazyKns/Perplexica</title>
    <updated>2024-08-04T01:45:04Z</updated>
    <id>tag:github.com,2024-08-04:/ItzCrazyKns/Perplexica</id>
    <link href="https://github.com/ItzCrazyKns/Perplexica" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Perplexica is an AI-powered search engine. It is an Open source alternative to Perplexity AI&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;🚀 Perplexica - An AI-powered search engine 🔎 &#xA; &lt;!-- omit in toc --&gt;&lt;/h1&gt; &#xA;&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/.assets/perplexica-screenshot.png&#34; alt=&#34;preview&#34;&gt;&lt;/p&gt; &#xA;&lt;h2&gt;Table of Contents &#xA; &lt;!-- omit in toc --&gt;&lt;/h2&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#overview&#34;&gt;Overview&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#preview&#34;&gt;Preview&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#features&#34;&gt;Features&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#installation&#34;&gt;Installation&lt;/a&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#getting-started-with-docker-recommended&#34;&gt;Getting Started with Docker (Recommended)&lt;/a&gt;&lt;/li&gt; &#xA;   &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#non-docker-installation&#34;&gt;Non-Docker Installation&lt;/a&gt;&lt;/li&gt; &#xA;   &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#ollama-connection-errors&#34;&gt;Ollama Connection Errors&lt;/a&gt;&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#using-as-a-search-engine&#34;&gt;Using as a Search Engine&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#one-click-deployment&#34;&gt;One-Click Deployment&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#upcoming-features&#34;&gt;Upcoming Features&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#support-us&#34;&gt;Support Us&lt;/a&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#donations&#34;&gt;Donations&lt;/a&gt;&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#contribution&#34;&gt;Contribution&lt;/a&gt;&lt;/li&gt; &#xA; &lt;li&gt;&lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#help-and-support&#34;&gt;Help and Support&lt;/a&gt;&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h2&gt;Overview&lt;/h2&gt; &#xA;&lt;p&gt;Perplexica is an open-source AI-powered searching tool or an AI-powered search engine that goes deep into the internet to find answers. Inspired by Perplexity AI, it&#39;s an open-source option that not just searches the web but understands your questions. It uses advanced machine learning algorithms like similarity searching and embeddings to refine results and provides clear answers with sources cited.&lt;/p&gt; &#xA;&lt;p&gt;Using SearxNG to stay current and fully open source, Perplexica ensures you always get the most up-to-date information without compromising your privacy.&lt;/p&gt; &#xA;&lt;p&gt;Want to know more about its architecture and how it works? You can read it &lt;a href=&#34;https://github.com/ItzCrazyKns/Perplexica/tree/master/docs/architecture/README.md&#34;&gt;here&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Preview&lt;/h2&gt; &#xA;&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/.assets/perplexica-preview.gif&#34; alt=&#34;video-preview&#34;&gt;&lt;/p&gt; &#xA;&lt;h2&gt;Features&lt;/h2&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;strong&gt;Local LLMs&lt;/strong&gt;: You can make use local LLMs such as Llama3 and Mixtral using Ollama.&lt;/li&gt; &#xA; &lt;li&gt;&lt;strong&gt;Two Main Modes:&lt;/strong&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Copilot Mode:&lt;/strong&gt; (In development) Boosts search by generating different queries to find more relevant internet sources. Like normal search instead of just using the context by SearxNG, it visits the top matches and tries to find relevant sources to the user&#39;s query directly from the page.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Normal Mode:&lt;/strong&gt; Processes your query and performs a web search.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;&lt;strong&gt;Focus Modes:&lt;/strong&gt; Special modes to better answer specific types of questions. Perplexica currently has 6 focus modes: &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;strong&gt;All Mode:&lt;/strong&gt; Searches the entire web to find the best results.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Writing Assistant Mode:&lt;/strong&gt; Helpful for writing tasks that does not require searching the web.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Academic Search Mode:&lt;/strong&gt; Finds articles and papers, ideal for academic research.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;YouTube Search Mode:&lt;/strong&gt; Finds YouTube videos based on the search query.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Wolfram Alpha Search Mode:&lt;/strong&gt; Answers queries that need calculations or data analysis using Wolfram Alpha.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Reddit Search Mode:&lt;/strong&gt; Searches Reddit for discussions and opinions related to the query.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt;&lt;strong&gt;Current Information:&lt;/strong&gt; Some search tools might give you outdated info because they use data from crawling bots and convert them into embeddings and store them in a index. Unlike them, Perplexica uses SearxNG, a metasearch engine to get the results and rerank and get the most relevant source out of it, ensuring you always get the latest information without the overhead of daily data updates.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;It has many more features like image and video search. Some of the planned features are mentioned in &lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/#upcoming-features&#34;&gt;upcoming features&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Installation&lt;/h2&gt; &#xA;&lt;p&gt;There are mainly 2 ways of installing Perplexica - With Docker, Without Docker. Using Docker is highly recommended.&lt;/p&gt; &#xA;&lt;h3&gt;Getting Started with Docker (Recommended)&lt;/h3&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt; &lt;p&gt;Ensure Docker is installed and running on your system.&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;Clone the Perplexica repository:&lt;/p&gt; &lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;git clone https://github.com/ItzCrazyKns/Perplexica.git&#xA;&lt;/code&gt;&lt;/pre&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;After cloning, navigate to the directory containing the project files.&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;Rename the &lt;code&gt;sample.config.toml&lt;/code&gt; file to &lt;code&gt;config.toml&lt;/code&gt;. For Docker setups, you need only fill in the following fields:&lt;/p&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt; &lt;p&gt;&lt;code&gt;OPENAI&lt;/code&gt;: Your OpenAI API key. &lt;strong&gt;You only need to fill this if you wish to use OpenAI&#39;s models&lt;/strong&gt;.&lt;/p&gt; &lt;/li&gt; &#xA;   &lt;li&gt; &lt;p&gt;&lt;code&gt;OLLAMA&lt;/code&gt;: Your Ollama API URL. You should enter it as &lt;code&gt;http://host.docker.internal:PORT_NUMBER&lt;/code&gt;. If you installed Ollama on port 11434, use &lt;code&gt;http://host.docker.internal:11434&lt;/code&gt;. For other ports, adjust accordingly. &lt;strong&gt;You need to fill this if you wish to use Ollama&#39;s models instead of OpenAI&#39;s&lt;/strong&gt;.&lt;/p&gt; &lt;/li&gt; &#xA;   &lt;li&gt; &lt;p&gt;&lt;code&gt;GROQ&lt;/code&gt;: Your Groq API key. &lt;strong&gt;You only need to fill this if you wish to use Groq&#39;s hosted models&lt;/strong&gt;.&lt;/p&gt; &lt;/li&gt; &#xA;   &lt;li&gt; &lt;p&gt;&lt;code&gt;ANTHROPIC&lt;/code&gt;: Your Anthropic API key. &lt;strong&gt;You only need to fill this if you wish to use Anthropic models&lt;/strong&gt;.&lt;/p&gt; &lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt;: You can change these after starting Perplexica from the settings dialog.&lt;/p&gt; &lt;/li&gt; &#xA;   &lt;li&gt; &lt;p&gt;&lt;code&gt;SIMILARITY_MEASURE&lt;/code&gt;: The similarity measure to use (This is filled by default; you can leave it as is if you are unsure about it.)&lt;/p&gt; &lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;Ensure you are in the directory containing the &lt;code&gt;docker-compose.yaml&lt;/code&gt; file and execute:&lt;/p&gt; &lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;docker compose up -d&#xA;&lt;/code&gt;&lt;/pre&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;Wait a few minutes for the setup to complete. You can access Perplexica at &lt;a href=&#34;http://localhost:3000&#34;&gt;http://localhost:3000&lt;/a&gt; in your web browser.&lt;/p&gt; &lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt;: After the containers are built, you can start Perplexica directly from Docker without having to open a terminal.&lt;/p&gt; &#xA;&lt;h3&gt;Non-Docker Installation&lt;/h3&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt;Install SearXNG and allow &lt;code&gt;JSON&lt;/code&gt; format in the SearXNG settings.&lt;/li&gt; &#xA; &lt;li&gt;Clone the repository and rename the &lt;code&gt;sample.config.toml&lt;/code&gt; file to &lt;code&gt;config.toml&lt;/code&gt; in the root directory. Ensure you complete all required fields in this file.&lt;/li&gt; &#xA; &lt;li&gt;Rename the &lt;code&gt;.env.example&lt;/code&gt; file to &lt;code&gt;.env&lt;/code&gt; in the &lt;code&gt;ui&lt;/code&gt; folder and fill in all necessary fields.&lt;/li&gt; &#xA; &lt;li&gt;After populating the configuration and environment files, run &lt;code&gt;npm i&lt;/code&gt; in both the &lt;code&gt;ui&lt;/code&gt; folder and the root directory.&lt;/li&gt; &#xA; &lt;li&gt;Install the dependencies and then execute &lt;code&gt;npm run build&lt;/code&gt; in both the &lt;code&gt;ui&lt;/code&gt; folder and the root directory.&lt;/li&gt; &#xA; &lt;li&gt;Finally, start both the frontend and the backend by running &lt;code&gt;npm run start&lt;/code&gt; in both the &lt;code&gt;ui&lt;/code&gt; folder and the root directory.&lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt;: Using Docker is recommended as it simplifies the setup process, especially for managing environment variables and dependencies.&lt;/p&gt; &#xA;&lt;p&gt;See the &lt;a href=&#34;https://github.com/ItzCrazyKns/Perplexica/tree/master/docs/installation&#34;&gt;installation documentation&lt;/a&gt; for more information like exposing it your network, etc.&lt;/p&gt; &#xA;&lt;h3&gt;Ollama Connection Errors&lt;/h3&gt; &#xA;&lt;p&gt;If you&#39;re encountering an Ollama connection error, it is likely due to the backend being unable to connect to Ollama&#39;s API. To fix this issue you can:&lt;/p&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;Check your Ollama API URL:&lt;/strong&gt; Ensure that the API URL is correctly set in the settings menu.&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;Update API URL Based on OS:&lt;/strong&gt;&lt;/p&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Windows:&lt;/strong&gt; Use &lt;code&gt;http://host.docker.internal:11434&lt;/code&gt;&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Mac:&lt;/strong&gt; Use &lt;code&gt;http://host.docker.internal:11434&lt;/code&gt;&lt;/li&gt; &#xA;   &lt;li&gt;&lt;strong&gt;Linux:&lt;/strong&gt; Use &lt;code&gt;http://&amp;lt;private_ip_of_host&amp;gt;:11434&lt;/code&gt;&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;p&gt;Adjust the port number if you&#39;re using a different one.&lt;/p&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;strong&gt;Linux Users - Expose Ollama to Network:&lt;/strong&gt;&lt;/p&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt; &lt;p&gt;Inside &lt;code&gt;/etc/systemd/system/ollama.service&lt;/code&gt;, you need to add &lt;code&gt;Environment=&#34;OLLAMA_HOST=0.0.0.0&#34;&lt;/code&gt;. Then restart Ollama by &lt;code&gt;systemctl restart ollama&lt;/code&gt;. For more information see &lt;a href=&#34;https://github.com/ollama/ollama/raw/main/docs/faq.md#setting-environment-variables-on-linux&#34;&gt;Ollama docs&lt;/a&gt;&lt;/p&gt; &lt;/li&gt; &#xA;   &lt;li&gt; &lt;p&gt;Ensure that the port (default is 11434) is not blocked by your firewall.&lt;/p&gt; &lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;h2&gt;Using as a Search Engine&lt;/h2&gt; &#xA;&lt;p&gt;If you wish to use Perplexica as an alternative to traditional search engines like Google or Bing, or if you want to add a shortcut for quick access from your browser&#39;s search bar, follow these steps:&lt;/p&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt;Open your browser&#39;s settings.&lt;/li&gt; &#xA; &lt;li&gt;Navigate to the &#39;Search Engines&#39; section.&lt;/li&gt; &#xA; &lt;li&gt;Add a new site search with the following URL: &lt;code&gt;http://localhost:3000/?q=%s&lt;/code&gt;. Replace &lt;code&gt;localhost&lt;/code&gt; with your IP address or domain name, and &lt;code&gt;3000&lt;/code&gt; with the port number if Perplexica is not hosted locally.&lt;/li&gt; &#xA; &lt;li&gt;Click the add button. Now, you can use Perplexica directly from your browser&#39;s search bar.&lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;h2&gt;One-Click Deployment&lt;/h2&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://repocloud.io/details/?app_id=267&#34;&gt;&lt;img src=&#34;https://d16t0pc4846x52.cloudfront.net/deploylobe.svg?sanitize=true&#34; alt=&#34;Deploy to RepoCloud&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;h2&gt;Upcoming Features&lt;/h2&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; checked disabled&gt; Add settings page&lt;/li&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; checked disabled&gt; Adding support for local LLMs&lt;/li&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; checked disabled&gt; History Saving features&lt;/li&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; checked disabled&gt; Introducing various Focus Modes&lt;/li&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; disabled&gt; Finalizing Copilot Mode&lt;/li&gt; &#xA; &lt;li&gt;&lt;input type=&#34;checkbox&#34; disabled&gt; Adding Discover&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h2&gt;Support Us&lt;/h2&gt; &#xA;&lt;p&gt;If you find Perplexica useful, consider giving us a star on GitHub. This helps more people discover Perplexica and supports the development of new features. Your support is greatly appreciated.&lt;/p&gt; &#xA;&lt;h3&gt;Donations&lt;/h3&gt; &#xA;&lt;p&gt;We also accept donations to help sustain our project. If you would like to contribute, you can use the following options to donate. Thank you for your support!&lt;/p&gt; &#xA;&lt;table&gt; &#xA; &lt;thead&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;th&gt;Ethereum&lt;/th&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/thead&gt; &#xA; &lt;tbody&gt; &#xA;  &lt;tr&gt; &#xA;   &lt;td&gt;Address: &lt;code&gt;0xB025a84b2F269570Eb8D4b05DEdaA41D8525B6DD&lt;/code&gt;&lt;/td&gt; &#xA;  &lt;/tr&gt; &#xA; &lt;/tbody&gt; &#xA;&lt;/table&gt; &#xA;&lt;h2&gt;Contribution&lt;/h2&gt; &#xA;&lt;p&gt;Perplexica is built on the idea that AI and large language models should be easy for everyone to use. If you find bugs or have ideas, please share them in via GitHub Issues. For more information on contributing to Perplexica you can read the &lt;a href=&#34;https://raw.githubusercontent.com/ItzCrazyKns/Perplexica/master/CONTRIBUTING.md&#34;&gt;CONTRIBUTING.md&lt;/a&gt; file to learn more about Perplexica and how you can contribute to it.&lt;/p&gt; &#xA;&lt;h2&gt;Help and Support&lt;/h2&gt; &#xA;&lt;p&gt;If you have any questions or feedback, please feel free to reach out to us. You can create an issue on GitHub or join our Discord server. There, you can connect with other users, share your experiences and reviews, and receive more personalized help. &lt;a href=&#34;https://discord.gg/EFwsmQDgAu&#34;&gt;Click here&lt;/a&gt; to join the Discord server. To discuss matters outside of regular support, feel free to contact me on Discord at &lt;code&gt;itzcrazykns&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;p&gt;Thank you for exploring Perplexica, the AI-powered search engine designed to enhance your search experience. We are constantly working to improve Perplexica and expand its capabilities. We value your feedback and contributions which help us make Perplexica even better. Don&#39;t forget to check back for updates and new features!&lt;/p&gt;</summary>
  </entry>
</feed>