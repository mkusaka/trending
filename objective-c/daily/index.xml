<?xml version="1.0" encoding="UTF-8"?><feed xmlns="http://www.w3.org/2005/Atom">
  <title>GitHub Objective-C Daily Trending</title>
  <id>http://mshibanami.github.io/GitHubTrendingRSS</id>
  <updated>2022-08-02T01:50:05Z</updated>
  <subtitle>Daily Trending of Objective-C in GitHub</subtitle>
  <link href="http://mshibanami.github.io/GitHubTrendingRSS"></link>
  <entry>
    <title>PromiseKit/UIKit</title>
    <updated>2022-08-02T01:50:05Z</updated>
    <id>tag:github.com,2022-08-02:/PromiseKit/UIKit</id>
    <link href="https://github.com/PromiseKit/UIKit" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Promises for Swift &amp; ObjC&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;PromiseKit UIKit Extensions &lt;img src=&#34;https://travis-ci.org/PromiseKit/UIKit.svg?branch=master&#34; alt=&#34;Build Status&#34;&gt;&lt;/h1&gt; &#xA;&lt;p&gt;This project adds promises to Apple’s UIKit framework.&lt;/p&gt; &#xA;&lt;p&gt;This project supports Swift 3.0, 3.1, 3.2 and 4.0; iOS 9, 10 and 11; tvOS 10 and 11; CocoaPods and Carthage; Xcode 8.0, 8.1, 8.2, 8.3 and 9.0.&lt;/p&gt; &#xA;&lt;h2&gt;CocoaPods&lt;/h2&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ruby&#34;&gt;pod &#34;PromiseKit/UIKit&#34;, &#34;~&amp;gt; 6.0&#34;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The extensions are built into &lt;code&gt;PromiseKit.framework&lt;/code&gt; thus &lt;code&gt;import PromiseKit&lt;/code&gt; is all that is needed.&lt;/p&gt; &#xA;&lt;h2&gt;Carthage&lt;/h2&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ruby&#34;&gt;github &#34;PromiseKit/UIKit&#34; ~&amp;gt; 3.0&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The extensions are built into their own framework:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// swift&#xA;import PromiseKit&#xA;import PMKUIKit&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objc&#34;&gt;// objc&#xA;@import PromiseKit;&#xA;@import PMKUIKit;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h1&gt;&lt;code&gt;UIImagePickerController&lt;/code&gt;&lt;/h1&gt; &#xA;&lt;p&gt;Due to iOS 10 requiring an entry in your app’s &lt;code&gt;Info.plist&lt;/code&gt; for any usage of &lt;code&gt;UIImagePickerController&lt;/code&gt; (even if you don’t actually call it directly), we have removed UIImagePickerController from the default &lt;code&gt;UIKit&lt;/code&gt; pod. To use it you must add an additional subspec:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ruby&#34;&gt;pod &#34;PromiseKit/UIImagePickerController&#34;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Sorry, but there’s not an easier way.&lt;/p&gt;</summary>
  </entry>
  <entry>
    <title>BoltsFramework/Bolts-ObjC</title>
    <updated>2022-08-02T01:50:05Z</updated>
    <id>tag:github.com,2022-08-02:/BoltsFramework/Bolts-ObjC</id>
    <link href="https://github.com/BoltsFramework/Bolts-ObjC" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Bolts is a collection of low-level libraries designed to make developing mobile apps easier.&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;Bolts&lt;/h1&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://travis-ci.org/BoltsFramework/Bolts-ObjC&#34;&gt;&lt;img src=&#34;https://img.shields.io/travis/BoltsFramework/Bolts-ObjC/master.svg?style=flat&#34; alt=&#34;Build Status&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://codecov.io/github/BoltsFramework/Bolts-ObjC?branch=master&#34;&gt;&lt;img src=&#34;https://codecov.io/github/BoltsFramework/Bolts-ObjC/coverage.svg?branch=master&#34; alt=&#34;Coverage Status&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://cocoapods.org/pods/Bolts&#34;&gt;&lt;img src=&#34;https://img.shields.io/cocoapods/p/Bolts.svg?style=flat&#34; alt=&#34;Pod Platform&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://github.com/BoltsFramework/Bolts-ObjC/raw/master/LICENSE&#34;&gt;&lt;img src=&#34;https://img.shields.io/cocoapods/l/Bolts.svg?style=flat&#34; alt=&#34;Pod License&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://www.versioneye.com/objective-c/bolts/references&#34;&gt;&lt;img src=&#34;https://www.versioneye.com/objective-c/bolts/reference_badge.svg?style=flat&#34; alt=&#34;Reference Status&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;&lt;a href=&#34;https://cocoapods.org/pods/Bolts&#34;&gt;&lt;img src=&#34;https://img.shields.io/cocoapods/v/Bolts.svg?style=flat&#34; alt=&#34;Pod Version&#34;&gt;&lt;/a&gt; &lt;a href=&#34;https://github.com/Carthage/Carthage&#34;&gt;&lt;img src=&#34;https://img.shields.io/badge/Carthage-compatible-4BC51D.svg?style=flat&#34; alt=&#34;Carthage compatible&#34;&gt;&lt;/a&gt;&lt;/p&gt; &#xA;&lt;p&gt;Bolts is a collection of low-level libraries designed to make developing mobile apps easier. Bolts was designed by Parse and Facebook for our own internal use, and we have decided to open source these libraries to make them available to others. Using these libraries does not require using any Parse services. Nor do they require having a Parse or Facebook developer account.&lt;/p&gt; &#xA;&lt;p&gt;Bolts includes:&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&#34;Tasks&#34;, which make organization of complex asynchronous code more manageable. A task is kind of like a JavaScript Promise, but available for iOS and Android.&lt;/li&gt; &#xA; &lt;li&gt;An implementation of the &lt;a href=&#34;http://applinks.org/&#34;&gt;App Links protocol&lt;/a&gt;, helping you link to content in other apps and handle incoming deep-links.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;For more information, see the &lt;a href=&#34;http://boltsframework.github.io/docs/ios/&#34;&gt;Bolts iOS API Reference&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h1&gt;Tasks&lt;/h1&gt; &#xA;&lt;p&gt;To build a truly responsive iOS application, you must keep long-running operations off of the UI thread, and be careful to avoid blocking anything the UI thread might be waiting on. This means you will need to execute various operations in the background. To make this easier, we&#39;ve added a class called &lt;code&gt;BFTask&lt;/code&gt;. A task represents the result of an asynchronous operation. Typically, a &lt;code&gt;BFTask&lt;/code&gt; is returned from an asynchronous function and gives the ability to continue processing the result of the task. When a task is returned from a function, it&#39;s already begun doing its job. A task is not tied to a particular threading model: it represents the work being done, not where it is executing. Tasks have many advantages over other methods of asynchronous programming, such as callbacks. &lt;code&gt;BFTask&lt;/code&gt; is not a replacement for &lt;code&gt;NSOperation&lt;/code&gt; or GCD. In fact, they play well together. But tasks do fill in some gaps that those technologies don&#39;t address.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;BFTask&lt;/code&gt; takes care of managing dependencies for you. Unlike using &lt;code&gt;NSOperation&lt;/code&gt; for dependency management, you don&#39;t have to declare all dependencies before starting a &lt;code&gt;BFTask&lt;/code&gt;. For example, imagine you need to save a set of objects and each one may or may not require saving child objects. With an &lt;code&gt;NSOperation&lt;/code&gt;, you would normally have to create operations for each of the child saves ahead of time. But you don&#39;t always know before you start the work whether that&#39;s going to be necessary. That can make managing dependencies with &lt;code&gt;NSOperation&lt;/code&gt; very painful. Even in the best case, you have to create your dependencies before the operations that depend on them, which results in code that appears in a different order than it executes. With &lt;code&gt;BFTask&lt;/code&gt;, you can decide during your operation&#39;s work whether there will be subtasks and return the other task in just those cases.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;BFTasks&lt;/code&gt; release their dependencies. &lt;code&gt;NSOperation&lt;/code&gt; strongly retains its dependencies, so if you have a queue of ordered operations and sequence them using dependencies, you have a leak, because every operation gets retained forever. &lt;code&gt;BFTasks&lt;/code&gt; release their callbacks as soon as they are run, so everything cleans up after itself. This can reduce memory use, and simplify memory management.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;BFTasks&lt;/code&gt; keep track of the state of finished tasks: It tracks whether there was a returned value, the task was cancelled, or if an error occurred. It also has convenience methods for propagating errors. With &lt;code&gt;NSOperation&lt;/code&gt;, you have to build all of this stuff yourself.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;BFTasks&lt;/code&gt; don&#39;t depend on any particular threading model. So it&#39;s easy to have some tasks perform their work with an operation queue, while others perform work using blocks with GCD. These tasks can depend on each other seamlessly.&lt;/li&gt; &#xA; &lt;li&gt;Performing several tasks in a row will not create nested &#34;pyramid&#34; code as you would get when using only callbacks.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;BFTasks&lt;/code&gt; are fully composable, allowing you to perform branching, parallelism, and complex error handling, without the spaghetti code of having many named callbacks.&lt;/li&gt; &#xA; &lt;li&gt;You can arrange task-based code in the order that it executes, rather than having to split your logic across scattered callback functions.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;p&gt;For the examples in this doc, assume there are async versions of some common Parse methods, called &lt;code&gt;saveAsync:&lt;/code&gt; and &lt;code&gt;findAsync:&lt;/code&gt; which return a &lt;code&gt;Task&lt;/code&gt;. In a later section, we&#39;ll show how to define these functions yourself.&lt;/p&gt; &#xA;&lt;h2&gt;The &lt;code&gt;continueWithBlock&lt;/code&gt; Method&lt;/h2&gt; &#xA;&lt;p&gt;Every &lt;code&gt;BFTask&lt;/code&gt; has a method named &lt;code&gt;continueWithBlock:&lt;/code&gt; which takes a continuation block. A continuation is a block that will be executed when the task is complete. You can then inspect the task to check if it was successful and to get its result.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;[[self saveAsync:obj] continueWithBlock:^id(BFTask *task) {&#xA;  if (task.isCancelled) {&#xA;    // the save was cancelled.&#xA;  } else if (task.error) {&#xA;    // the save failed.&#xA;  } else {&#xA;    // the object was saved successfully.&#xA;    PFObject *object = task.result;&#xA;  }&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;self.saveAsync(obj).continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  if task.isCancelled() {&#xA;    // the save was cancelled.&#xA;  } else if task.error != nil {&#xA;    // the save failed.&#xA;  } else {&#xA;    // the object was saved successfully.&#xA;    var object = task.result() as PFObject&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;BFTasks use Objective-C blocks, so the syntax should be pretty straightforward. Let&#39;s look closer at the types involved with an example.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;/**&#xA; * Gets an NSString asynchronously.&#xA; */&#xA;- (BFTask *)getStringAsync {&#xA;  // Let&#39;s suppose getNumberAsync returns a BFTask whose result is an NSNumber.&#xA;  return [[self getNumberAsync] continueWithBlock:^id(BFTask *task) {&#xA;    // This continuation block takes the NSNumber BFTask as input,&#xA;    // and provides an NSString as output.&#xA;&#xA;    NSNumber *number = task.result;&#xA;    return [NSString stringWithFormat:@&#34;%@&#34;, number];&#xA;  )];&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;/**&#xA; * Gets an NSString asynchronously.&#xA; */&#xA;func getStringAsync() -&amp;gt; BFTask {&#xA;  //Let&#39;s suppose getNumberAsync returns a BFTask whose result is an NSNumber.&#xA;  return self.getNumberAsync().continueWithBlock {&#xA;    (task: BFTask!) -&amp;gt; NSString in&#xA;    // This continuation block takes the NSNumber BFTask as input,&#xA;    // and provides an NSString as output.&#xA;&#xA;    let number = task.result() as NSNumber&#xA;    return NSString(format:&#34;%@&#34;, number)&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;In many cases, you only want to do more work if the previous task was successful, and propagate any errors or cancellations to be dealt with later. To do this, use the &lt;code&gt;continueWithSuccessBlock:&lt;/code&gt; method instead of &lt;code&gt;continueWithBlock:&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;[[self saveAsync:obj] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  // the object was saved successfully.&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;self.saveAsync(obj).continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  // the object was saved successfully.&#xA;  return nil&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Chaining Tasks Together&lt;/h2&gt; &#xA;&lt;p&gt;BFTasks are a little bit magical, in that they let you chain them without nesting. If you return a BFTask from &lt;code&gt;continueWithBlock:&lt;/code&gt;, then the task returned by &lt;code&gt;continueWithBlock:&lt;/code&gt; will not be considered finished until the new task returned from the new continuation block. This lets you perform multiple actions without incurring the pyramid code you would get with callbacks. Likewise, you can return a &lt;code&gt;BFTask&lt;/code&gt; from &lt;code&gt;continueWithSuccessBlock:&lt;/code&gt;. So, return a &lt;code&gt;BFTask&lt;/code&gt; to do more asynchronous work.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;PFQuery *query = [PFQuery queryWithClassName:@&#34;Student&#34;];&#xA;[query orderByDescending:@&#34;gpa&#34;];&#xA;[[[[[self findAsync:query] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  NSArray *students = task.result;&#xA;  PFObject *valedictorian = [students objectAtIndex:0];&#xA;  [valedictorian setObject:@YES forKey:@&#34;valedictorian&#34;];&#xA;  return [self saveAsync:valedictorian];&#xA;}] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  PFObject *valedictorian = task.result;&#xA;  return [self findAsync:query];&#xA;}] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  NSArray *students = task.result;&#xA;  PFObject *salutatorian = [students objectAtIndex:1];&#xA;  [salutatorian setObject:@YES forKey:@&#34;salutatorian&#34;];&#xA;  return [self saveAsync:salutatorian];&#xA;}] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  // Everything is done!&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;var query = PFQuery(className:&#34;Student&#34;)&#xA;query.orderByDescending(&#34;gpa&#34;)&#xA;findAsync(query).continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  let students = task.result() as NSArray&#xA;  var valedictorian = students.objectAtIndex(0) as PFObject&#xA;  valedictorian[&#34;valedictorian&#34;] = true&#xA;  return self.saveAsync(valedictorian)&#xA;}.continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  var valedictorian = task.result() as PFObject&#xA;  return self.findAsync(query)&#xA;}.continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  let students = task.result() as NSArray&#xA;  var salutatorian = students.objectAtIndex(1) as PFObject&#xA;  salutatorian[&#34;salutatorian&#34;] = true&#xA;  return self.saveAsync(salutatorian)&#xA;}.continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  // Everything is done!&#xA;  return nil&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Error Handling&lt;/h2&gt; &#xA;&lt;p&gt;By carefully choosing whether to call &lt;code&gt;continueWithBlock:&lt;/code&gt; or &lt;code&gt;continueWithSuccessBlock:&lt;/code&gt;, you can control how errors are propagated in your application. Using &lt;code&gt;continueWithBlock:&lt;/code&gt; lets you handle errors by transforming them or dealing with them. You can think of failed tasks kind of like throwing an exception. In fact, if you throw an exception inside a continuation, the resulting task will be faulted with that exception.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;PFQuery *query = [PFQuery queryWithClassName:@&#34;Student&#34;];&#xA;[query orderByDescending:@&#34;gpa&#34;];&#xA;[[[[[self findAsync:query] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  NSArray *students = task.result;&#xA;  PFObject *valedictorian = [students objectAtIndex:0];&#xA;  [valedictorian setObject:@YES forKey:@&#34;valedictorian&#34;];&#xA;  // Force this callback to fail.&#xA;  return [BFTask taskWithError:[NSError errorWithDomain:@&#34;example.com&#34;&#xA;                                                   code:-1&#xA;                                               userInfo:nil]];&#xA;}] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  // Now this continuation will be skipped.&#xA;  PFQuery *valedictorian = task.result;&#xA;  return [self findAsync:query];&#xA;}] continueWithBlock:^id(BFTask *task) {&#xA;  if (task.error) {&#xA;    // This error handler WILL be called.&#xA;    // The error will be the NSError returned above.&#xA;    // Let&#39;s handle the error by returning a new value.&#xA;    // The task will be completed with nil as its value.&#xA;    return nil;&#xA;  }&#xA;  // This will also be skipped.&#xA;  NSArray *students = task.result;&#xA;  PFObject *salutatorian = [students objectAtIndex:1];&#xA;  [salutatorian setObject:@YES forKey:@&#34;salutatorian&#34;];&#xA;  return [self saveAsync:salutatorian];&#xA;}] continueWithSuccessBlock:^id(BFTask *task) {&#xA;  // Everything is done! This gets called.&#xA;  // The task&#39;s result is nil.&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;var query = PFQuery(className:&#34;Student&#34;)&#xA;query.orderByDescending(&#34;gpa&#34;)&#xA;findAsync(query).continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  let students = task.result() as NSArray&#xA;  var valedictorian = students.objectAtIndex(0) as PFObject&#xA;  valedictorian[&#34;valedictorian&#34;] = true&#xA;  //Force this callback to fail.&#xA;  return BFTask(error:NSError(domain:&#34;example.com&#34;,&#xA;                              code:-1, userInfo: nil))&#xA;}.continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  //Now this continuation will be skipped.&#xA;  var valedictorian = task.result() as PFObject&#xA;  return self.findAsync(query)&#xA;}.continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  if task.error != nil {&#xA;    // This error handler WILL be called.&#xA;    // The error will be the NSError returned above.&#xA;    // Let&#39;s handle the error by returning a new value.&#xA;    // The task will be completed with nil as its value.&#xA;    return nil&#xA;  }&#xA;  // This will also be skipped.&#xA;  let students = task.result() as NSArray&#xA;  var salutatorian = students.objectAtIndex(1) as PFObject&#xA;  salutatorian[&#34;salutatorian&#34;] = true&#xA;  return self.saveAsync(salutatorian)&#xA;}.continueWithSuccessBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  // Everything is done! This gets called.&#xA;  // The tasks result is nil.&#xA;  return nil&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;It&#39;s often convenient to have a long chain of success callbacks with only one error handler at the end.&lt;/p&gt; &#xA;&lt;h2&gt;Creating Tasks&lt;/h2&gt; &#xA;&lt;p&gt;When you&#39;re getting started, you can just use the tasks returned from methods like &lt;code&gt;findAsync:&lt;/code&gt; or &lt;code&gt;saveAsync:&lt;/code&gt;. However, for more advanced scenarios, you may want to make your own tasks. To do that, you create a &lt;code&gt;BFTaskCompletionSource&lt;/code&gt;. This object will let you create a new &lt;code&gt;BFTask&lt;/code&gt;, and control whether it gets marked as finished or cancelled. After you create a &lt;code&gt;BFTaskCompletionSource&lt;/code&gt;, you&#39;ll need to call &lt;code&gt;setResult:&lt;/code&gt;, &lt;code&gt;setError:&lt;/code&gt;, or &lt;code&gt;cancel&lt;/code&gt; to trigger its continuations.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;- (BFTask *)successAsync {&#xA;  BFTaskCompletionSource *successful = [BFTaskCompletionSource taskCompletionSource];&#xA;  [successful setResult:@&#34;The good result.&#34;];&#xA;  return successful.task;&#xA;}&#xA;&#xA;- (BFTask *)failAsync {&#xA;  BFTaskCompletionSource *failed = [BFTaskCompletionSource taskCompletionSource];&#xA;  [failed setError:[NSError errorWithDomain:@&#34;example.com&#34; code:-1 userInfo:nil]];&#xA;  return failed.task;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;func successAsync() -&amp;gt; BFTask {&#xA;  var successful = BFTaskCompletionSource()&#xA;  successful.setResult(&#34;The good result.&#34;)&#xA;  return successful.task&#xA;}&#xA;&#xA;func failAsync() -&amp;gt; BFTask {&#xA;  var failed = BFTaskCompletionSource()&#xA;  failed.setError(NSError(domain:&#34;example.com&#34;, code:-1, userInfo:nil))&#xA;  return failed.task&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;If you know the result of a task at the time it is created, there are some convenience methods you can use.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;BFTask *successful = [BFTask taskWithResult:@&#34;The good result.&#34;];&#xA;&#xA;BFTask *failed = [BFTask taskWithError:anError];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;let successful = BFTask(result:&#34;The good result&#34;)&#xA;&#xA;let failed = BFTask(error:anError)&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Creating Async Methods&lt;/h2&gt; &#xA;&lt;p&gt;With these tools, it&#39;s easy to make your own asynchronous functions that return tasks. For example, you can make a task-based version of &lt;code&gt;fetchAsync:&lt;/code&gt; easily.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;- (BFTask *) fetchAsync:(PFObject *)object {&#xA;  BFTaskCompletionSource *task = [BFTaskCompletionSource taskCompletionSource];&#xA;  [object fetchInBackgroundWithBlock:^(PFObject *object, NSError *error) {&#xA;    if (!error) {&#xA;      [task setResult:object];&#xA;    } else {&#xA;      [task setError:error];&#xA;    }&#xA;  }];&#xA;  return task.task;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;func fetchAsync(object: PFObject) -&amp;gt; BFTask {&#xA;  var task = BFTaskCompletionSource()&#xA;  object.fetchInBackgroundWithBlock {&#xA;    (object: PFObject?, error: NSError?) -&amp;gt; Void in&#xA;    if error == nil {&#xA;      task.setResult(object)&#xA;    } else {&#xA;      task.setError(error)&#xA;    }&#xA;  }&#xA;  return task.task&#xA;}&#xA;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;It&#39;s similarly easy to create &lt;code&gt;saveAsync:&lt;/code&gt;, &lt;code&gt;findAsync:&lt;/code&gt; or &lt;code&gt;deleteAsync:&lt;/code&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Tasks in Series&lt;/h2&gt; &#xA;&lt;p&gt;&lt;code&gt;BFTasks&lt;/code&gt; are convenient when you want to do a series of tasks in a row, each one waiting for the previous to finish. For example, imagine you want to delete all of the comments on your blog.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;PFQuery *query = [PFQuery queryWithClassName:@&#34;Comments&#34;];&#xA;[query whereKey:@&#34;post&#34; equalTo:@123];&#xA;&#xA;[[[self findAsync:query] continueWithBlock:^id(BFTask *task) {&#xA;  NSArray *results = task.result;&#xA;&#xA;  // Create a trivial completed task as a base case.&#xA;  BFTask *task = [BFTask taskWithResult:nil];&#xA;  for (PFObject *result in results) {&#xA;    // For each item, extend the task with a function to delete the item.&#xA;    task = [task continueWithBlock:^id(BFTask *task) {&#xA;      // Return a task that will be marked as completed when the delete is finished.&#xA;      return [self deleteAsync:result];&#xA;    }];&#xA;  }&#xA;  return task;&#xA;}] continueWithBlock:^id(BFTask *task) {&#xA;  // Every comment was deleted.&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;var query = PFQuery(className:&#34;Comments&#34;)&#xA;query.whereKey(&#34;post&#34;, equalTo:123)&#xA;findAsync(query).continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  let results = task.result() as NSArray&#xA;&#xA;  // Create a trivial completed task as a base case.&#xA;  let task = BFTask(result:nil)&#xA;  for result : PFObject in results {&#xA;    // For each item, extend the task with a function to delete the item.&#xA;    task = task.continueWithBlock {&#xA;      (task: BFTask!) -&amp;gt; BFTask in&#xA;      return self.deleteAsync(result)&#xA;    }&#xA;  }&#xA;  return task&#xA;}.continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  // Every comment was deleted.&#xA;  return nil&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Tasks in Parallel&lt;/h2&gt; &#xA;&lt;p&gt;You can also perform several tasks in parallel, using the &lt;code&gt;taskForCompletionOfAllTasks:&lt;/code&gt; method. You can start multiple operations at once, and use &lt;code&gt;taskForCompletionOfAllTasks:&lt;/code&gt; to create a new task that will be marked as completed when all of its input tasks are completed. The new task will be successful only if all of the passed-in tasks succeed. Performing operations in parallel will be faster than doing them serially, but may consume more system resources and bandwidth.&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Objective-C&#xA;PFQuery *query = [PFQuery queryWithClassName:@&#34;Comments&#34;];&#xA;[query whereKey:@&#34;post&#34; equalTo:@123];&#xA;&#xA;[[[self findAsync:query] continueWithBlock:^id(BFTask *results) {&#xA;  // Collect one task for each delete into an array.&#xA;  NSMutableArray *tasks = [NSMutableArray array];&#xA;  for (PFObject *result in results) {&#xA;    // Start this delete immediately and add its task to the list.&#xA;    [tasks addObject:[self deleteAsync:result]];&#xA;  }&#xA;  // Return a new task that will be marked as completed when all of the deletes are&#xA;  // finished.&#xA;  return [BFTask taskForCompletionOfAllTasks:tasks];&#xA;}] continueWithBlock:^id(BFTask *task) {&#xA;  // Every comment was deleted.&#xA;  return nil;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// Swift&#xA;var query = PFQuery(className:&#34;Comments&#34;)&#xA;query.whereKey(&#34;post&#34;, equalTo:123)&#xA;&#xA;findAsync(query).continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; BFTask in&#xA;  // Collect one task for each delete into an array.&#xA;  var tasks = NSMutableArray.array()&#xA;  var results = task.result() as NSArray&#xA;  for result : PFObject! in results {&#xA;    // Start this delete immediately and add its task to the list.&#xA;    tasks.addObject(self.deleteAsync(result))&#xA;  }&#xA;  // Return a new task that will be marked as completed when all of the deletes&#xA;  // are finished.&#xA;  return BFTask(forCompletionOfAllTasks:tasks)&#xA;}.continueWithBlock {&#xA;  (task: BFTask!) -&amp;gt; AnyObject! in&#xA;  // Every comment was deleted.&#xA;  return nil&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Task Executors&lt;/h2&gt; &#xA;&lt;p&gt;Both &lt;code&gt;continueWithBlock:&lt;/code&gt; and &lt;code&gt;continueWithSuccessBlock:&lt;/code&gt; methods have another form that takes an instance of &lt;code&gt;BFExecutor&lt;/code&gt;. These are &lt;code&gt;continueWithExecutor:withBlock:&lt;/code&gt; and &lt;code&gt;continueWithExecutor:withSuccessBlock:&lt;/code&gt;. These methods allow you to control how the continuation is executed. The default executor will dispatch to GCD, but you can provide your own executor to schedule work onto a different thread. For example, if you want to continue with work on the UI thread:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Create a BFExecutor that uses the main thread.&#xA;BFExecutor *myExecutor = [BFExecutor executorWithBlock:^void(void(^block)()) {&#xA;  dispatch_async(dispatch_get_main_queue(), block);&#xA;}];&#xA;&#xA;// And use the Main Thread Executor like this. The executor applies only to the new&#xA;// continuation being passed into continueWithBlock.&#xA;[[self fetchAsync:object] continueWithExecutor:myExecutor withBlock:^id(BFTask *task) {&#xA;    myTextView.text = [object objectForKey:@&#34;name&#34;];&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;For common cases, such as dispatching on the main thread, we have provided default implementations of &lt;code&gt;BFExecutor&lt;/code&gt;. These include &lt;code&gt;defaultExecutor&lt;/code&gt;, &lt;code&gt;immediateExecutor&lt;/code&gt;, &lt;code&gt;mainThreadExecutor&lt;/code&gt;, &lt;code&gt;executorWithDispatchQueue:&lt;/code&gt;, and &lt;code&gt;executorWithOperationQueue:&lt;/code&gt;. For example:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;// Continue on the Main Thread, using a built-in executor.&#xA;[[self fetchAsync:object] continueWithExecutor:[BFExecutor mainThreadExecutor] withBlock:^id(BFTask *task) {&#xA;    myTextView.text = [object objectForKey:@&#34;name&#34;];&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Task Cancellation&lt;/h2&gt; &#xA;&lt;p&gt;It&#39;s generally bad design to keep track of the &lt;code&gt;BFTaskCompletionSource&lt;/code&gt; for cancellation. A better model is to create a &#34;cancellation token&#34; at the top level, and pass that to each async function that you want to be part of the same &#34;cancelable operation&#34;. Then, in your continuation blocks, you can check whether the cancellation token has been cancelled and bail out early by returning a &lt;code&gt;[BFTask cancelledTask]&lt;/code&gt;. For example:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;- (void)doSomethingComplicatedAsync:(MYCancellationToken *)cancellationToken {&#xA;    [[self doSomethingAsync:cancellationToken] continueWithBlock:^{&#xA;        if (cancellationToken.isCancelled) {&#xA;            return [BFTask cancelledTask];&#xA;        }&#xA;        // Do something that takes a while.&#xA;        return result;&#xA;    }];&#xA;}&#xA;&#xA;// Somewhere else.&#xA;MYCancellationToken *cancellationToken = [[MYCancellationToken alloc] init];&#xA;[obj doSomethingComplicatedAsync:cancellationToken];&#xA;&#xA;// When you get bored...&#xA;[cancellationToken cancel];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; The cancellation token implementation should be thread-safe. We are likely to add some concept like this to Bolts at some point in the future.&lt;/p&gt; &#xA;&lt;h1&gt;App Links&lt;/h1&gt; &#xA;&lt;p&gt;&lt;a href=&#34;http://applinks.org/&#34;&gt;App Links&lt;/a&gt; provide a cross-platform mechanism that allows a developer to define and publish a deep-linking scheme for their content, allowing other apps to link directly to an experience optimized for the device they are running on. Whether you are building an app that receives incoming links or one that may link out to other apps&#39; content, Bolts provides tools to simplify implementation of the &lt;a href=&#34;http://applinks.org/documentation&#34;&gt;App Links protocol&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;h2&gt;Handling an App Link&lt;/h2&gt; &#xA;&lt;p&gt;The most common case will be making your app receive App Links. In-linking will allow your users to quickly access the richest, most native-feeling presentation of linked content on their devices. Bolts makes it easy to handle an inbound App Link (as well as general inbound deep-links) by providing utilities for processing an incoming URL.&lt;/p&gt; &#xA;&lt;p&gt;For example, you can use the &lt;code&gt;BFURL&lt;/code&gt; utility class to parse an incoming URL in your &lt;code&gt;AppDelegate&lt;/code&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;- (BOOL)application:(UIApplication *)application&#xA;            openURL:(NSURL *)url&#xA;  sourceApplication:(NSString *)sourceApplication&#xA;         annotation:(id)annotation {&#xA;    BFURL *parsedUrl = [BFURL URLWithInboundURL:url sourceApplication:sourceApplication];&#xA;&#xA;    // Use the target URL from the App Link to locate content.&#xA;    if ([parsedUrl.targetURL.pathComponents[1] isEqualToString:@&#34;profiles&#34;]) {&#xA;        // Open a profile viewer.&#xA;    }&#xA;&#xA;    // You can also check the query string easily.&#xA;    NSString *query = parsedUrl.targetQueryParameters[@&#34;query&#34;];&#xA;&#xA;    // Apps that have existing deep-linking support and map their App Links to existing&#xA;    // deep-linking functionality may instead want to perform these operations on the input URL.&#xA;    // Use the target URL from the App Link to locate content.&#xA;    if ([parsedUrl.inputURL.pathComponents[1] isEqualToString:@&#34;profiles&#34;]) {&#xA;        // Open a profile viewer.&#xA;    }&#xA;&#xA;    // You can also check the query string easily.&#xA;    NSString *query = parsedUrl.inputQueryParameters[@&#34;query&#34;];&#xA;&#xA;    // Apps can easily check the Extras and App Link data from the App Link as well.&#xA;    NSString *fbAccessToken = parsedUrl.appLinkExtras[@&#34;fb_access_token&#34;];&#xA;    NSDictionary *refererData = parsedUrl.appLinkExtras[@&#34;referer&#34;];&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;Navigating to a URL&lt;/h2&gt; &#xA;&lt;p&gt;Following an App Link allows your app to provide the best user experience (as defined by the receiving app) when a user navigates to a link. Bolts makes this process simple, automating the steps required to follow a link:&lt;/p&gt; &#xA;&lt;ol&gt; &#xA; &lt;li&gt;Resolve the App Link by getting the App Link metadata from the HTML at the URL specified.&lt;/li&gt; &#xA; &lt;li&gt;Step through App Link targets relevant to the device being used, checking whether the app that can handle the target is present on the device.&lt;/li&gt; &#xA; &lt;li&gt;If an app is present, build a URL with the appropriate al_applink_data specified and navigate to that URL.&lt;/li&gt; &#xA; &lt;li&gt;Otherwise, open the browser with the original URL specified.&lt;/li&gt; &#xA;&lt;/ol&gt; &#xA;&lt;p&gt;In the simplest case, it takes just one line of code to navigate to a URL that may have an App Link:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;[BFAppLinkNavigation navigateToURLInBackground:url];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Adding App and Navigation Data&lt;/h3&gt; &#xA;&lt;p&gt;Under most circumstances, the data that will need to be passed along to an app during a navigation will be contained in the URL itself, so that whether or not the app is actually installed on the device, users are taken to the correct content. Occasionally, however, apps will want to pass along data that is relevant for app-to-app navigation, or will want to augment the App Link protocol with information that might be used by the app to adjust how the app should behave (e.g. showing a link back to the referring app).&lt;/p&gt; &#xA;&lt;p&gt;If you want to take advantage of these features, you can break apart the navigation process. First, you must have an App Link to which you wish to navigate:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;[[BFAppLinkNavigation resolveAppLinkInBackground:url] continueWithSuccessBlock:^id(BFTask *task) {&#xA;    BFAppLink *link = task.result;&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Then, you can build an App Link request with any additional data you would like and navigate:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;BFAppLinkNavigation *navigation = [BFAppLinkNavigation navigationWithAppLink:link&#xA;                                                                      extras:@{ @&#34;access_token&#34;: @&#34;t0kEn&#34; }&#xA;                                                                 appLinkData:@{ @&#34;ref&#34;: @&#34;12345&#34; }];&#xA;NSError *error = nil;&#xA;[navigation navigate:&amp;amp;error];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;Resolving App Link Metadata&lt;/h3&gt; &#xA;&lt;p&gt;Bolts allows for custom App Link resolution, which may be used as a performance optimization (e.g. caching the metadata) or as a mechanism to allow developers to use a centralized index for obtaining App Link metadata. A custom App Link resolver just needs to be able to take a URL and return a &lt;code&gt;BFAppLink&lt;/code&gt; containing the ordered list of &lt;code&gt;BFAppLinkTarget&lt;/code&gt;s that are applicable for this device. Bolts provides one of these out of the box that performs this resolution on the device using a hidden &lt;del&gt;UIWebView&lt;/del&gt; WKWebview.&lt;/p&gt; &#xA;&lt;p&gt;You can use any resolver that implements the &lt;code&gt;BFAppLinkResolving&lt;/code&gt; protocol by using one of the overloads on &lt;code&gt;BFAppLinkNavigation&lt;/code&gt;:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;[BFAppLinkNavigation navigateToURLInBackground:url&#xA;                                      resolver:resolver];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;Alternatively, a you can swap out the default resolver to be used by the built-in APIs:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;[BFAppLinkNavigation setDefaultResolver:resolver];&#xA;[BFAppLinkNavigation navigateToURLInBackground:url];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h2&gt;App Link Return-to-Referer View&lt;/h2&gt; &#xA;&lt;p&gt;When an application is opened via an App Link, a banner allowing the user to &#34;Touch to return to &#xA; &lt;calling app&gt;&#xA;  &#34; should be displayed. The &#xA;  &lt;code&gt;BFAppLinkReturnToRefererView&lt;/code&gt; provides this functionality. It will take an incoming App Link and parse the referer information to display the appropriate calling app name.&#xA; &lt;/calling&gt;&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;- (void)viewDidLoad {&#xA;  [super viewDidLoad];&#xA;&#xA;  // Perform other view initialization.&#xA;&#xA;  self.returnToRefererController = [[BFAppLinkReturnToRefererController alloc] init];&#xA;&#xA;  // self.returnToRefererView is a BFAppLinkReturnToRefererView.&#xA;  // You may initialize the view either by loading it from a NIB or programmatically.&#xA;  self.returnToRefererController.view = self.returnToRefererView;&#xA;&#xA;  // If you have a UINavigationController in the view, then the bar must be shown above it.&#xA;  [self.returnToRefererController]&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The following code assumes that the view controller has an &lt;code&gt;openedAppLinkURL&lt;/code&gt; &lt;code&gt;NSURL&lt;/code&gt; property that has already been populated with the URL used to open the app. You can then do something like this to show the view:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;- (void)viewWillAppear {&#xA;  [super viewWillAppear];&#xA;&#xA;  // Show only if you have a back AppLink.&#xA;  [self.returnToRefererController showViewForRefererURL:self.openedAppLinkURL];&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;In a navigation-controller view hierarchy, the banner should be displayed above the navigation bar, and &lt;code&gt;BFAppLinkReturnToRefererController&lt;/code&gt; provides an &lt;code&gt;initForDisplayAboveNavController&lt;/code&gt; method to assist with this.&lt;/p&gt; &#xA;&lt;h2&gt;Analytics&lt;/h2&gt; &#xA;&lt;p&gt;Bolts introduces Measurement Event. App Links posts three different Measurement Event notifications to the application, which can be caught and integrated with existing analytics components in your application.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt;&lt;code&gt;al_nav_out&lt;/code&gt; — Raised when your app switches out to an App Links URL.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;al_nav_in&lt;/code&gt; — Raised when your app opens an incoming App Links URL.&lt;/li&gt; &#xA; &lt;li&gt;&lt;code&gt;al_ref_back_out&lt;/code&gt; — Raised when your app returns back the referrer app using the built-in top navigation back bar view.&lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h3&gt;Listen for App Links Measurement Events&lt;/h3&gt; &#xA;&lt;p&gt;There are other analytics tools that are integrated with Bolts&#39; App Links events, but you can also listen for these events yourself:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objective-c&#34;&gt;[[NSNotificationCenter defaultCenter] addObserverForName:BFMeasurementEventNotificationName object:nil queue:nil usingBlock:^(NSNotification *note) {&#xA;    NSDictionary *event = note.userInfo;&#xA;    NSDictionary *eventData = event[BFMeasurementEventArgsKey];&#xA;    // Integrate to your logging/analytics component.&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;h3&gt;App Links Event Fields&lt;/h3&gt; &#xA;&lt;p&gt;App Links Measurement Events sends additional information from App Links Intents in flattened string key value pairs. Here are some of the useful fields for the three events.&lt;/p&gt; &#xA;&lt;ul&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;code&gt;al_nav_in&lt;/code&gt;&lt;/p&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;code&gt;inputURL&lt;/code&gt;: the URL that opens the app.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;inputURLScheme&lt;/code&gt;: the scheme of &lt;code&gt;inputURL&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;refererURL&lt;/code&gt;: the URL that the referrer app added into &lt;code&gt;al_applink_data&lt;/code&gt;: &lt;code&gt;referer_app_link&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;refererAppName&lt;/code&gt;: the app name that the referrer app added to &lt;code&gt;al_applink_data&lt;/code&gt;: &lt;code&gt;referer_app_link&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;sourceApplication&lt;/code&gt;: the bundle of referrer application.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;targetURL&lt;/code&gt;: the &lt;code&gt;target_url&lt;/code&gt; field in &lt;code&gt;al_applink_data&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;version&lt;/code&gt;: App Links API version.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA; &lt;li&gt; &lt;p&gt;&lt;code&gt;al_nav_out&lt;/code&gt; / &lt;code&gt;al_ref_back_out&lt;/code&gt;&lt;/p&gt; &#xA;  &lt;ul&gt; &#xA;   &lt;li&gt;&lt;code&gt;outputURL&lt;/code&gt;: the URL used to open the other app (or browser). If there is an eligible app to open, this will be the custom scheme url/intent in &lt;code&gt;al_applink_data&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;outputURLScheme&lt;/code&gt;: the scheme of &lt;code&gt;outputURL&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;sourceURL&lt;/code&gt;: the URL of the page hosting App Links meta tags.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;sourceURLHost&lt;/code&gt;: the hostname of &lt;code&gt;sourceURL&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;success&lt;/code&gt;: &lt;code&gt;“1”&lt;/code&gt; to indicate success in opening the App Link in another app or browser; &lt;code&gt;“0”&lt;/code&gt; to indicate failure to open the App Link.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;type&lt;/code&gt;: &lt;code&gt;“app”&lt;/code&gt; for open in app, &lt;code&gt;“web”&lt;/code&gt; for open in browser; &lt;code&gt;“fail”&lt;/code&gt; when the success field is &lt;code&gt;“0”&lt;/code&gt;.&lt;/li&gt; &#xA;   &lt;li&gt;&lt;code&gt;version&lt;/code&gt;: App Links API version.&lt;/li&gt; &#xA;  &lt;/ul&gt; &lt;/li&gt; &#xA;&lt;/ul&gt; &#xA;&lt;h1&gt;Installation&lt;/h1&gt; &#xA;&lt;p&gt;You can download the latest framework files from our &lt;a href=&#34;https://github.com/BoltsFramework/Bolts-ObjC/releases&#34;&gt;Releases page&lt;/a&gt;.&lt;/p&gt; &#xA;&lt;p&gt;Bolts is also available through &lt;a href=&#34;https://cocoapods.org/&#34;&gt;CocoaPods&lt;/a&gt;. To install it simply add the following line to your Podfile:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code&gt;pod &#39;Bolts&#39;&#xA;&lt;/code&gt;&lt;/pre&gt;</summary>
  </entry>
  <entry>
    <title>PromiseKit/Social</title>
    <updated>2022-08-02T01:50:05Z</updated>
    <id>tag:github.com,2022-08-02:/PromiseKit/Social</id>
    <link href="https://github.com/PromiseKit/Social" rel="alternate"></link>
    <summary type="html">&lt;p&gt;Promises for Swift &amp; ObjC&lt;/p&gt;&lt;hr&gt;&lt;h1&gt;PromiseKit Social Extensions &lt;img src=&#34;https://travis-ci.org/PromiseKit/Social.svg?branch=master&#34; alt=&#34;Build Status&#34;&gt;&lt;/h1&gt; &#xA;&lt;p&gt;This project adds promises to Apple’s Social framework.&lt;/p&gt; &#xA;&lt;h2&gt;CocoaPods&lt;/h2&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ruby&#34;&gt;pod &#34;PromiseKit/Social&#34;, &#34;~&amp;gt; 6.0&#34;&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The extensions are built into &lt;code&gt;PromiseKit.framework&lt;/code&gt; thus nothing else is needed.&lt;/p&gt; &#xA;&lt;h2&gt;Carthage&lt;/h2&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-ruby&#34;&gt;github &#34;PromiseKit/Social&#34; ~&amp;gt; 3.0&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;p&gt;The extensions are built into their own framework:&lt;/p&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-swift&#34;&gt;// swift&#xA;import PromiseKit&#xA;import PMKSocial&#xA;&lt;/code&gt;&lt;/pre&gt; &#xA;&lt;pre&gt;&lt;code class=&#34;language-objc&#34;&gt;// objc&#xA;@import PromiseKit;&#xA;@import PMKSocial;&#xA;&lt;/code&gt;&lt;/pre&gt;</summary>
  </entry>
</feed>